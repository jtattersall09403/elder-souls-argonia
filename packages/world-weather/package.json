{
  "name": "@elder-souls/world-weather",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Deterministic province weather: seeded synoptic state machine driven by the world clock and climate fields, locally expressed per region. Pure data — no rendering dependency (world module 55 §98, Phase 8c).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "dependencies": {
    "@elder-souls/world-time": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.0.18"
  }
}
