/**
 * The one catalogue of player-visible text (engineering standard 4).
 *
 * `catalogue.ts` is the machinery — types, the builder and the checks it
 * enforces. `entries.ts` is the content. Read the header of `catalogue.ts` for
 * why this package exists.
 */
export * from "./catalogue.js";
export * from "./entries.js";
