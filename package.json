{
  "name": "elder-souls-argonia",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/repo-standards"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present && npm run weapons:reach -- --check",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev -w @elder-souls/combat-sandbox",
    "weapons:reach": "node tooling/asset-pipeline/scripts/bake-weapon-reach.mjs",
    "test:placement": "cd tooling/world-generation && python3 -m pytest -q worldgen/test_blueprint*.py worldgen/test_catalogue.py worldgen/test_grade_routes.py worldgen/test_plot_stats.py",
    "test:placement:slow": "cd tooling/world-generation && python3 -m pytest -q -m slow $(python3 -c \"import importlib.util;print('-n=auto' if importlib.util.find_spec('xdist') else '')\")"
  },
  "devDependencies": {
    "rolldown": "1.2.5"
  }
}
