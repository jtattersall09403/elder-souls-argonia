# tooling/bootstrap

Scripts that turn a fresh GitHub codespace, or an EC2 machine (§ EC2 below), into a working copy of the build VM. `.devcontainer/devcontainer.json` calls them. The plan behind them is `docs/research/infrastructure/codespaces-migration-plan.md` (Part 3).

The image comes first (`.devcontainer/Dockerfile`). It installs the system packages, rclone, rtk and the asset-pipeline toolchain under `/opt/es-tools`: Wine 11.13 wow64, the Windows build of Blender 4.4.3 with PyNifly V28.1.0 in its `addons_core`, and gltfpack 1.2. Every download is pinned by sha256. There is no X server: Wine and Blender run headless, as on the VM. Then three lifecycle scripts run automatically, in this order, and one script is run by hand:

- `on-create.sh` (onCreateCommand, no secrets, baked by the prebuild) holds only fast installs that cannot wait for input, because the codespace stays in "Provisioning" until it ends. It links `~/tools` to `/opt/es-tools`, where `pipeline/config/toolchain.json` looks, runs the pip installs (world-generation `requirements-test.txt` plus asset-pipeline `requirements.txt`), `npm ci` and `npx --yes playwright install --with-deps chromium`. Each step runs under `timeout` (1, 15, 20 and 15 minutes) with stdin from `/dev/null` and a non-interactive environment (`steps.sh`); its start, end, exit code and seconds go to stdout and `/workspaces/.es-bootstrap/on-create.log`, and a failed or timed-out step stops the script naming the step. Each step writes a marker in `~/.es-bootstrap/`; delete a marker to redo that step.
- `post-create.sh` (postCreateCommand, has secrets) writes the rclone `r2:` remote and `~/.config/nexus/api_key`, both mode 600. If the vault is missing, it clones `/workspaces/elder-scrolls-asset-pipeline` from the `vault-git` bundle and makes a local branch for every bundled branch (`snapshot-vault.sh` bundles `HEAD --branches --tags`, so a bundle re-made here carries them all). It then pulls the `base` tier, the `claude` tier and the `vault` tier (`vault-git`, skipped once the checkout exists, and `vault-worktree`), in that order, with no `--force`. It checks all five secrets below: a missing restore secret stops it with the names, a missing `CLAUDE_CODE_OAUTH_TOKEN` prints a warning (Claude Code will ask you to log in) and the restore goes on.
- `on-start.sh` (postStartCommand, every start) first runs `cache-links.sh` (§ Disk below), then is the one place that derives `ES_TUNNEL_URL` from `ES_STUDIO_PORT`. It replaces its block in `~/.bashrc` on every start (VS Code terminals are interactive bash and read it) and sets `env.ES_TUNNEL_URL` and `env.ES_STUDIO_PORT` in the repo's gitignored `.claude/settings.local.json` (merged, created with mode 600 when absent). The tunnel env lives only there and in `~/.bashrc`, never in `$CLAUDE_CONFIG_DIR/settings.json`, and the `claude-home` snapshot strips both keys from the `settings.json` it tars. It warns if `rtk` is missing.
- `first-run.sh` (by hand, once, from a codespace terminal after `post-create.sh` has finished): **run `bash tooling/bootstrap/first-run.sh` once before kit, settlement or chain work.** It reads the Wine, Blender and prefix paths from `pipeline/config/toolchain.json`, builds the prefix with `wineboot -i` (with `WINEDLLOVERRIDES="mscoree,mshtml="` and no display, so the Wine Mono and Gecko installers never open; the VM prefix has neither) and checks that Blender under Wine loads `io_scene_nifly` (about 1 minute; 15-minute limit; marker `~/.es-bootstrap/wine-prefix.done`). It then runs `vault-pull.sh --tier toolchain --tier cache` (every run; the pull skips parts it holds), prints `df` and `vault-pull.sh --free`, and a ready line. Log: `/workspaces/.es-bootstrap/first-run.log`.

`steps.sh` is the step runner `on-create.sh` and `first-run.sh` share (timeout, log, marker). `lib.sh` holds what the two vault scripts share: paths, manifest reads, and `fingerprint`, the sha256 of the sorted `path<TAB>size<TAB>mtime-seconds` lines of the files in a set.

`snapshot-vault.sh` writes `snapshot-manifest.json` (schemaVersion 1), which lists each part with its `id`, `object`, `bytes`, `sha256`, `root` (relative to the dev root), `tier` (base, claude, vault, chain, toolchain, cache or mod), `archivesPart`, `needsArchives`, `sourceBytes` (information only) and `sourceFingerprint` (the fingerprint of exactly the files the part tars; for `vault-git`, of the vault's refs). A new record is merged over the old one: a field this machine cannot see (an `archivesPart` whose archives were never pulled here) keeps its manifest value. A part is uploaded when its fingerprint differs from the manifest's; `--only <id>` uploads it regardless, `--dry-run` only reports, `--list --files` shows the tar entries. `--force` only re-uploads unchanged parts; it lifts no guard. Two guards, each with its own flag: on a codespace (a `.vault-pull/` marker folder exists) a part already in the manifest is uploaded only if it was pulled there (a part new to the manifest uploads; `--allow-partial` lifts it), and (`--allow-shrink` lifts it) a part whose files add up to less than half the manifest's `sourceBytes` is refused, except `vault-worktree`, `claude-home` and `claude-transcripts`, which shrink honestly (changes committed to the vault, Claude memory or transcripts pruned). Claude transcripts are backed up to the private bucket, never to git. While any `.vault-pull/<id>.done.inprogress` exists (an interrupted pull) it refuses to run and names the parts, and no tar holds a path matching `*.incoming-*` (the old staging name, kept as a guard). The Claude part reads `$CLAUDE_CONFIG_DIR` (default `~/.claude`).

`vault-pull.sh` restores snapshot parts from the R2 bucket. Each part is one uncompressed tar of paths relative to the dev root. It is streamed through `rclone cat | tar -x`, and the sha256 is checked on the same stream. A part whose object is a `.bundle` is saved as a file in `/workspaces/.vault-pull/` and is not extracted. Each pulled part leaves a marker `/workspaces/.vault-pull/<id>.done` of two lines, the part sha256 and the fingerprint of the files its tar held (taken right after extraction), plus a sidecar `<id>.done.files` listing those files. `snapshot-vault.sh` rewrites the marker the same way (`write_marker` in `lib.sh`) after it uploads a part from a codespace, A pull into a root that already exists is staged in `.vault-pull/staging/<id>` (same disk, outside every repo): only once the sha256 matches are the old version's stale files deleted and the new files moved in, so a failed stream leaves the live root and its marker untouched. Eviction runs the same local-change check over the whole root (a file no marker lists, such as a newly downloaded archive, counts as a change): such a root is kept ("kept <root>: local changes") and the next least recently used one goes instead. A pull writes `<marker>.inprogress` before it extracts and removes it after the marker; `.inprogress` records `phase=stream` or `phase=swap`; a staged pull interrupted after its sha256 verified (`swap`) is rolled forward to the new version; otherwise the next pull of an interrupted part removes its staging dir; after a staged pull the live files and old marker stay (still the previous version), after an in-place first pull the files its lists name (no others) are deleted too; then it pulls without `--force`. `.vault-pull/` is created only by a pull that writes. An unknown tier or part id stops with "unknown tier/part: <name>".

To pull a mod folder: `bash tooling/bootstrap/vault-pull.sh mod-sources/<folder>`. Add `--with-archives` for its original archives; a part that has `needsArchives` set gets them anyway. `--tier toolchain` fetches vanilla Skyrim, and only that (`first-run.sh` pulls it). `vault-worktree` holds only the vault's changed and untracked files outside every other part's root, so each file lives in exactly one part. Run `snapshot-vault.sh` (it skips unchanged parts) at the end of any session that changed the vault. Commit tooling/bootstrap/snapshot-manifest.json straight after every snapshot run. Tiers (the repo parts hold only git-ignored files under their own roots, never a tracked file; no `node_modules/` except `repo-water-recovery`, which is one):

| tier | parts | pulled | evicted |
|---|---|---|---|
| base | `skyrim-source-misc`, `vault-extra` (the vault's git-ignored files outside every other part's root, but `build/`: the KotM text corpus under `derived/text/`, `shots/`), `repo-studio-public` (the studio's public data), `repo-wg-products` (world-generation chain receipts, blueprint maps and route/report products), `repo-asset-kits-meta` (the non-GLB sidecars of `tooling/asset-pipeline/output/kits`: what the settlement compile and the footprint check read) | by `post-create.sh` | never |
| claude | `claude-home` (also the repo's `.claude/settings.local.json`, filtered, restored by `post-create.sh` only where the repo has none), `claude-transcripts` (this project's session transcripts, subagent and workflow dirs, tool results: its whole Claude project dir but `memory/`) | by `post-create.sh` | never |
| vault | `vault-git`, `vault-worktree` | once, by `post-create.sh` | never |
| chain | `mod-sources/tamriel-worldspaces-118678` (the base heightfield) | on demand, by whoever runs the terrain chain (`vault-pull.sh --tier chain`) | never |
| toolchain | `vanilla` | by `first-run.sh` (or on demand) | never |
| cache | `vault-output`, `repo-asset-build`, `repo-asset-output` (the rest of `tooling/asset-pipeline/output` minus `sheets/`: the raw kit GLBs used by kit and settlement WORK, i.e. measuring, assembly renders, the interiors index; the studio uses the compressed kits tracked in `apps/world-studio/public/kits`), `repo-wg-mesh-cache`, `repo-water-recovery` | by `first-run.sh`, or on demand (by id or `--tier cache`): kit builds, kit and settlement work (needs the raw kit GLBs in `repo-asset-output`) and the miners | like mod: least recently used, never with local changes, never a git-tracked file |
| mod | every other `mod-sources/<folder>` and its `.archives`, `bmv` | on demand | yes |

World-generation `survey-cache` (rebuilt in about 13 s) and `mesh-cache-work` (scratch) are not carried. `npm run site:compose` rebuilds `site/`. The codespace sets none of `ES_ASSET_PIPELINE_ROOT`, `ELDER_SOULS_ASSET_ROOT` and `ES_VAULT_ROOT`, because each has two meanings in the code (see PROGRESS row); every resolver finds the vault as the repo's sibling. `post-create.sh` stops if any is set, makes the relative links `tooling/asset-pipeline/skyrim-source/{Data,extracted,mod-sources}` into the vault (as on the VM), and checks that `worldgen.vault`, the asset pipeline's `vault_path` and the kit footprints resolve (a link whose tier is not pulled yet is reported as dangling). An upload streams to `snapshot/v1/_incoming/<object>`; unchanged content is dropped there, a change moves the current object to `snapshot/v1/_prev/<object>` and the upload into place (server-side), and the manifest records `prevSha256`; the next run finishes a swap a crash cut short. `vault-pull.sh` falls back to `_prev` when the object no longer matches its manifest. The manifest must be committed and pushed after every snapshot; only one previous version is kept. The vault's deleted-but-uncommitted paths travel in `vault-worktree` as `.vault-worktree-deletions` at the vault root (always present, empty when none); the pull deletes those files from the fresh clone and then removes the list. The vault tier is create-only: `vault-worktree` is extracted once, into the checkout `post-create.sh` cloned (marker `.vault-pull/vault-clone.done`); any later `--tier vault` exits 0 with "vault tier is restored once at create; the vault is edited on one machine at a time (hand over with snapshot-vault.sh)". The vault is edited on one machine at a time and has no GitHub remote: hand over by running `snapshot-vault.sh` on the old machine, committing the manifest, then creating the codespace. `--list` shows every part, `--free` shows free space and the pulled parts, and `--evict <id>` drops a mod part's whole folder. In `--free`, the `vault-clone` row prints `?` for tier and size: its marker (`vault-clone.done`) records the initial git clone of the vault, not a manifest part, so there is no tier or byte count to look up. The parts sharing a root are evicted together, and only if every one of them is tier mod or cache, so `tamriel-worldspaces-118678.archives` is never evicted (it shares its folder with the chain-tier heightfield part). When a pull needs room (its size plus 2 GiB), the least recently used such folders go first. Before extracting, a pull stops if the part's own files changed since its marker, or, with no marker, if any of them already exists here (files added later, such as Claude's session logs, never count). The message is "local changes in <root>: snapshot them first (snapshot-vault.sh --only <id>) or pass --force". With no marker, the part's file list comes from its sidecar object `snapshot/v1/<id>.files` (manifest `filesObject`, a few KB; `snapshot-vault.sh` writes it with every tar, `--backfill-filelists` for existing parts), and a listed file that git tracks and holds unchanged from HEAD (as a fresh clone leaves it) is not a local change; a missing sidecar refuses. `post-create.sh` therefore pulls `vault-worktree` without `--force`. Overrides: `ES_DEVROOT`, `ES_SNAPSHOT_MANIFEST`, `ES_RCLONE_REMOTE`, `ES_VAULT_PULL_RESERVE`, and for tests `ES_PULL_FREE_LIMIT_BYTES` (treats the dev root as a disk of that size).

The Codespaces secrets, scoped to this repo, are `ES_SNAPSHOT_ACCESS_KEY_ID`, `ES_SNAPSHOT_SECRET_ACCESS_KEY`, `ES_SNAPSHOT_ENDPOINT` (with no bucket suffix), `NEXUS_API_KEY` and `CLAUDE_CODE_OAUTH_TOKEN`. Claude Code reads the last one straight from the environment.

`restore-mods.sh` is the Nexus fallback for when the bucket is lost. Its header documents it.

## Disk: the mod pool and caches live on /tmp

Measured on the 4-core codespace, 2026-09-25: `/workspaces` is a 32 GB volume (`/dev/loop4`, 5.8 GB free before this change, 10.1 GB after), `/tmp` a separate 118 GB one (`/dev/sda1`, 109 GB free). The mod pool alone is over 30 GB (bmv 9.65 GB, drjacopo-3d-grass-library 9.2, here-there-be-monsters 4.87, king-of-the-murkmire 3.4, tropical-skyrim 1.46, ...), so it cannot live on `/workspaces`. The rule: **everything re-pullable and big goes on `/tmp/es-cache`, linked from where the tools read it**; `/workspaces` keeps the repo, the vault checkout, the base tier, the heightfield (tier chain) and `.claude-home`.

`cache-links.sh` (run by `on-start.sh` on every start, idempotent) makes:

| path (relative to /workspaces) | link target |
|---|---|
| every manifest root whose parts are all tier `mod` (`elder-scrolls-asset-pipeline/skyrim-source/mod-sources/<folder>`, `elder-souls-argonia/tooling/asset-pipeline/black-marsh-mod-source`) | `/tmp/es-cache/mod-sources/<folder>` |
| `elder-souls-argonia/tooling/world-generation/output/mesh-cache` (the miners' NIF mesh cache) | `/tmp/es-cache/mesh-cache/world-generation` |
| `elder-souls-argonia/tooling/asset-pipeline/build` (kit build data roots) | `/tmp/es-cache/builds/asset-pipeline` |

A real folder already at one of those paths is moved there first (copy, then delete). `vault_path.py`, the miners and `build_kit.py` read through the links unchanged. A mod folder is linked only while it holds files (a tool or test asking "is this mod here?" still gets "no" for an unpulled one); the two caches are always linked. `vault-pull.sh` links a planned root just before it extracts into it (`cache-links.sh --root`), always stages a linked root on the link's volume (`/tmp/es-cache/.staging`: GNU tar 1.35 refuses to extract through a link leading outside `-C`) and moves the files in through the link, counts free space and evicts per volume (`--free` prints both), and an eviction removes the target and the link. `snapshot-vault.sh` lists a linked root's files (`find -H`; for a repo root, instead of `git ls-files`) and tars it as a directory (`tar -h`), so a snapshot from a codespace carries the files, never the link. The tamriel-worldspaces folder stays on `/workspaces` (it shares its root with the chain-tier heightfield part).

When `/tmp` is emptied (GitHub's docs say at every stop, the owner's 2026-09-25 brief says at a rebuild; not measured here, and the scripts handle either), `on-start.sh` drops the vault-pull markers of every linked root whose target is empty, removes the emptied mod links and re-creates the cache targets, so `vault-pull.sh <id>` pulls a part again on demand; `first-run.sh` re-pulls the cache tier. Links inside the repo that `.gitignore` misses (a `dir/` rule never matches a link) go into `.git/info/exclude`. `cache-links.sh` does nothing where `/tmp` shares the dev root's volume; `ES_CACHE_ROOT` moves the cache root, `ES_CACHE_LINKS=0` turns it off.

## Resource guard

Owner rulings 2026-09-25, after two crashes of the 4-core codespace at 97-100 % CPU. Three layers, all in `tooling/repo-standards/`:

- `cpu_watchdog.sh` (the logic and thresholds are in `cpu_watchdog.py`'s docstring) is a daemon `on-start.sh` starts on every start (nohup, setsid, one instance per machine by flock on `/tmp/es-jobs/watchdog.lock`). Every 2 s it reads whole-machine CPU from `/proc/stat`. Above 85 % for three samples it SIGSTOPs the heaviest throttleable process, one per sample, until a sample is under 70 %; once CPU has stayed under 60 % for 10 s it SIGCONTs them one at a time, oldest first. Every 30 s it kills (SIGTERM, SIGKILL 10 s later) any `rtk` older than 120 s with no live child and any Blender, `wb.py`, miner, `build_kit`, vitest or node test worker orphaned (ppid 1) for 10 min. The editor server and its extension hosts, the VS Code tunnel CLI (`code tunnel`), sshd, the `claude` CLI and init are never touched. Log `/tmp/es-jobs/watchdog.log` (one line per action: pid, command, CPU, reason); `cpu_watchdog.sh --status` prints the state and the stopped list, `--stop` ends it and continues everything it stopped. A restarted watchdog re-adopts what its predecessor left stopped. Start it from a shell that is not niced: it inherits the priority.
- `job_guard.sh <lane> -- <command>` runs every heavy job: it waits for load, disk and memory headroom, takes one of max(1, floor(nproc/2) - 1) slots and runs the job under `memwatch.sh` with `nice -n 10 ionice -c3 taskset -c <upper half of the cores>` (2-3 here), so cores 0-1 stay free for the editor tunnel and the CLI.
- `jobs.mjs` caps parallel work at `ES_JOBS`, default max(1, floor(nproc/2)): `preflight.mjs` runs at most that many gates at once and that many pytest workers, and `run-workspaces.mjs` that many workspaces, each vitest pool with `VITEST_MAX_WORKERS` set to it. The caps multiply, so the CPU bound is the pin: when not already under job_guard, both run their children at nice 10 on the upper half of the cores (`pinPrefix`). Under job_guard, `memwatch.sh` exports its ceiling and preflight keeps each gate's ceiling 0.25 GiB below it, so a gate is killed and reported before the whole preflight. `npm run preflight -- --paths <pathspec...>` runs only the gates whose inputs the changed files touch (`preflight_select.mjs`).

Tests: `test_cpu_watchdog.py` (scripted samples; `ES_WD_LIVE=1` adds a 20 s run with real `yes` hogs), `preflight_select.test.mjs`, `test_shell_guard.py`.

## Resume

A crashed session's lanes resume from their own transcripts (owner 2026-09-25). `.claude/settings.json` carries this SessionStart hook, beside the token report:

```json
{ "type": "command", "command": "python3 tooling/repo-standards/lane_resume.py --brief 2>/dev/null || true", "timeout": 30 }
```

It prints nothing when every subagent of the last 3 days finished, and nothing in a headless `claude -p` session; else one line per unfinished lane and the instruction to relaunch each with `lane_resume.py --packet <agent-id>` as its brief (PROGRESS.md protocol 5). Printing a packet claims the lane (kept in `lane-resume-dismissed.json` in the project dir), so a second session is not told to relaunch it. The transcripts are in `$CLAUDE_CONFIG_DIR/projects/-workspaces-elder-souls-argonia/`, which the `claude-transcripts` part backs up; the tool's docstring says how it decides "unfinished".

## EC2 (browser VS Code, no ssh)

The same scripts run natively on an AWS EC2 machine (Ubuntu 24.04, no container), with the repo at the same path, `/workspaces/elder-souls-argonia`, so every path, the Claude project folder and the snapshot keys stay as they are. You reach it through VS Code in the browser (vscode.dev) over a VS Code tunnel. Nothing needs ssh, a key pair or an open port. The EC2 files:

- `ec2-user-data.sh` is pasted into the launch form. At first boot it creates the user `es` (with passwordless sudo), makes `/workspaces`, copies `tooling/bootstrap` from GitHub, runs `install-toolchain.sh --host --owner es`, and installs the three scripts below into `/opt/es/`. It also sets up `es-on-start.service`, which runs `on-start.sh` at every boot once the repo exists, and the idle timer. It ends by writing `/opt/es/USER-DATA-DONE`. Log: `/var/log/es-user-data.log`.
- `install-toolchain.sh` installs the codespace image's toolchain, and the codespace image runs it too: `.devcontainer/Dockerfile` calls `install-toolchain.sh --image --owner vscode`, so the devcontainer and EC2 share one list of packages and pins (`test_ec2.py`, run by `npm test`, checks this). With `--host` it adds what the devcontainer features gave a codespace: Node 22, gh, Claude Code, the VS Code CLI `code` (sha256 pinned; the tunnel service updates it afterwards), and a Python venv at `/opt/es/venv`, first on PATH, because stock Ubuntu's `python3` refuses `pip install`. It also writes `/etc/profile.d/es.sh`: the three containerEnv variables, plus `/workspaces/.es-machine.env` and `/workspaces/.es-secrets.env` read into every shell.
- `ec2-stage2.sh` (installed as `/opt/es/stage2.sh`) signs the tunnel in to GitHub and installs it as a user service of `es` named `es-argonia`.
- `ec2-stage3.sh` (installed as `/opt/es/stage3.sh`) does GitHub sign-in for pushing, clones the repo on `dev`, checks the secrets file and writes `ES_CACHE_LINKS=0` into `/workspaces/.es-machine.env`. That setting matters because there is one disk and `/tmp` is emptied at boot, so the mod pool stays in place. It then runs `on-create.sh`, `post-create.sh`, `first-run.sh`, `vault-pull.sh --tier mod --tier chain` (the whole pool, about 42 GiB) and `on-start.sh`. Every step skips what is done, so re-run it after a failure.
- `idle-stop.sh` (installed as `/opt/es/idle-stop.sh`, run by `es-idle-stop.timer` every 10 minutes) powers the machine off after 30 idle minutes in a row. Idle means all four hold: no `job_guard` slot is held, no Claude transcript changed in the last 30 minutes, the 1-minute load is under 0.5, and the tunnel has no client connection. Settings: `/etc/es/idle.env` (`ES_IDLE_MINUTES`, default 30). Change on a running machine: `sudo sed -i 's/^ES_IDLE_MINUTES=.*/ES_IDLE_MINUTES=30/' /etc/es/idle.env`. Log: `/var/log/es-idle.log`, one line per check. Add `--dry-run` to check without stopping.

**Owner steps**, once, in the AWS console and the browser:

1. **Launch the machine.** Sign in to the AWS console, set the region (top right) to Europe (London), open EC2 and click Launch instance.
   - Name: `es-argonia`.
   - Image: Ubuntu Server 24.04 LTS, 64-bit (x86).
   - Instance type: `m7i.2xlarge`.
   - Key pair: "Proceed without a key pair".
   - Network settings: click Edit and create a security group whose only inbound rule is type SSH with source the prefix list `com.amazonaws.eu-west-2.ec2-instance-connect` (the rule it offers says "Anywhere": change the source). The browser shell in step 3 needs this rule; you delete it after step 3, and the tunnel needs no inbound rule.
   - Storage: 250 GiB, gp3.
   - Advanced details: set "Shutdown behavior" to Stop. Under "User data", paste the whole of [ec2-user-data.sh](ec2-user-data.sh). No IAM role is needed.
   - Click Launch instance. You should see the instance go to "Running".
2. **Wait for the first boot to finish** (about 20 minutes). Select the instance, then Actions > Monitor and troubleshoot > Get system log, and refresh now and then. You should see a line `ES USER-DATA DONE` near the bottom.
3. **Connect the editor tunnel.** Select the instance, click Connect, choose "EC2 Instance Connect" and click Connect. A black terminal opens in the browser. Type `sudo -iu es bash /opt/es/stage2.sh` and press Enter. It prints a github.com address and an 8-character code. Open the address, enter the code and approve. You should see `Open https://vscode.dev/tunnel/es-argonia` at the end. Close the terminal tab.
   - Then remove the SSH rule: Security tab > the security group > Edit inbound rules > Delete > Save. You should see "Inbound rules (0)". To use the browser shell again later, add the same rule back for the session.
4. **Open the editor.** Go to https://vscode.dev/tunnel/es-argonia and sign in with the same GitHub account. Open the folder `/workspaces`. You should see the folder open, with a terminal available (Terminal > New Terminal).
5. **Add the secrets.** In the editor, create the file `/workspaces/.es-secrets.env` with these five lines, fill in the values after each `=` and save:
   ```
   ES_SNAPSHOT_ACCESS_KEY_ID=
   ES_SNAPSHOT_SECRET_ACCESS_KEY=
   ES_SNAPSHOT_ENDPOINT=
   NEXUS_API_KEY=
   CLAUDE_CODE_OAUTH_TOKEN=
   ```
   Then run `chmod 600 /workspaces/.es-secrets.env` in the terminal. You should see no output from `chmod`. The values are the same as the Codespaces secrets; the file lives outside the repo, so git never sees it.
6. **Restore everything.** In the terminal, run `bash /opt/es/stage3.sh`. It first prints a github.com address and a code for GitHub sign-in; approve it as in step 3. It then runs for about 40 minutes. You should see `[stage3] done. The machine is ready.` followed by the studio steps. Then open the folder `/workspaces/elder-souls-argonia`.
7. **Record the studio address, once.** Open the Ports panel (the "Ports" tab next to Terminal), click "Forward a Port", type `8081` and press Enter. Copy the address it shows (`https://....devtunnels.ms`) and leave it Private. In the terminal run `echo 'ES_TUNNEL_URL=<the address>' >> /workspaces/.es-machine.env`, then `bash tooling/bootstrap/on-start.sh`. Open a new terminal and run `npm run studio`. You should see the studio when you open that address. If the address changes after a restart, replace that line in `/workspaces/.es-machine.env` and run `on-start.sh` again.
8. **Add the backstop alarm** (in case the idle check misses something). Select the instance, then Actions > Monitor and troubleshoot > Manage CloudWatch alarms > Create an alarm. Choose CPU utilization, Average, period 5 minutes, "less than or equal to" 3 %, for 24 consecutive periods (2 hours), and the alarm action "Stop". You should see the alarm listed with state OK or "Insufficient data".
9. **Stop and start.** Instance state > Stop instance when you are done; the idle check does the same after 30 quiet minutes. A stopped machine costs only its disk (no compute charge), and everything on the disk stays. Instance state > Start instance brings it back. After about a minute the tunnel is up again at https://vscode.dev/tunnel/es-argonia. You should see the instance "Stopped" or "Running" in the list.

Other machine settings go in `/workspaces/.es-machine.env`, one `NAME=value` per line: `ES_TUNNEL_URL`, `ES_CACHE_LINKS`, and optionally `ES_GIT_NAME` and `ES_GIT_EMAIL` for the commit identity. Without them, stage 3 uses the GitHub login and its noreply address.

## Leaving Codespaces

The vault is edited on one machine at a time. Hand it over before the codespace goes:

1. On the codespace, run `bash tooling/bootstrap/snapshot-vault.sh`, then commit `tooling/bootstrap/snapshot-manifest.json`.
2. Push `dev`, so the EC2 machine clones the new manifest and these scripts.
3. Build the EC2 machine (§ EC2 above) and check that the studio loads there.
4. Then delete the codespace (github.com/codespaces > ... > Delete). Do not edit the vault on it after step 1.
