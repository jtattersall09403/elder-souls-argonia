[pytest]
; pipeline/blender/ holds scripts executed inside Blender (import bpy); the
; *_test.py naming there is not a pytest test. Collect only the pipeline tests.
testpaths = pipeline
norecursedirs = pipeline/blender prototypes
