[pytest]
testpaths = worldgen
markers =
    real_index: opts out of a suite's synthetic autouse fixtures; the test
        measures the real committed data
    xdist_group: pin a module to one xdist worker (`--dist=loadgroup`), for
        tests whose fixtures load hundreds of MB: one load, not one per worker
