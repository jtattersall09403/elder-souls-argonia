{
 "schemaVersion": 1,
 "about": "Modules below the freeze gate that still re-derive water instead of reading the record (decision 0066). Each row is owned by the chunk that ports it; test_record_reads.py fails on a module missing here that re-derives, and on a row whose module is clean (delete the row). The list only shrinks.",
 "modules": {
  "audit_place_semantics": {
   "portedBy": "16g",
   "why": "\"floodBand\": hyd[\"floodBand\"],"
  },
  "compile_minor_routes": {
   "portedBy": "16e",
   "why": "cost = np.where(s.flood >= 2, cost * COST_FLOOD, cost)"
  },
  "compile_minor_waterways": {
   "portedBy": "16e",
   "why": "cost = boat_cost_surface(ocean, s.lakes, s.river_band, s.tidal, s.wetlands)"
  },
  "compile_scatter": {
   "portedBy": "16f",
   "why": "np.asarray(Image.open(province / \"water\" / \"water-surface.png\").convert(\"RGB\")),"
  },
  "compile_settlement": {
   "portedBy": "16h",
   "why": "band = int(survey.flood[row, col])"
  },
  "hostility_frequency": {
   "portedBy": "16g",
   "why": "f\"`water/water-surface.png` is at or below zero, which comes to \""
  },
  "macro_plot": {
   "portedBy": "16g",
   "why": "wet = bool(s.wetlands[row, col]) or bool(s.flood[row, col] >= 2)"
  },
  "rebake_landcover": {
   "portedBy": "16f",
   "why": "salinity=npz[\"salinity\"], twi=npz[\"twi\"], wetlands=npz[\"wetlands\"],"
  },
  "settlement_ground_control": {
   "portedBy": "16h",
   "why": "DEFAULT_WATER_SURFACE = REPO_ROOT / \"apps\" / \"world-studio\" / \"public\" / \"provin"
  },
  "site_dossier": {
   "portedBy": "16g",
   "why": "flood = s.flood[sl][mask]"
  },
  "terrain_request_postconditions": {
   "portedBy": "16d",
   "why": "water_default = vault.parent / \"water-pass1.npz\""
  },
  "terrain_scour": {
   "portedBy": "16g",
   "why": "wet=(s.wetlands | (s.flood >= 2)"
  },
  "water_crossings": {
   "portedBy": "16e",
   "why": "npz = np.load(cw.DEFAULT_HEIGHTS.parent.parent / \"hydrology-pass1.npz\")"
  }
 }
}
