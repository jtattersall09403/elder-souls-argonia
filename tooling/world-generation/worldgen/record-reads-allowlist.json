{
 "schemaVersion": 1,
 "about": "Modules below the freeze gate that still re-derive water instead of reading the record (decision 0066). Each row is owned by the chunk that ports it; test_record_reads.py fails on a module missing here that re-derives, and on a row whose module is clean (delete the row). The list only shrinks. EMPTY since 16h: every module below the gate reads the record. A new row may only be added with an owner ruling.",
 "modules": {}
}
