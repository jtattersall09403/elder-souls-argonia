# 16k hand-off (2026-09-25, session switch)

Owner rulings given at the close of the planning session, applied by the
next session ("continue 16k slice 1a from the hand-off"). Read
[PROGRESS.md](../../PROGRESS.md), decision 0099, the
[16k brief](../../phases/16-foundation-and-places/16k-place-loop.md), then this.

## Rulings

1. **Type list approved with two changes.** Type 8 is a WHOLE city, never a
   district; the owner is hands-on for every major city and for the
   early-game (opening-scene) places. Add a type 9 "early-game location
   (owner-guided)": name one or two from the quest plan's Milestone-1
   provisions ([docs/quests/20-world-provisions.md](../../quests/20-world-provisions.md))
   and decision 0062's opening-scene list.
2. **Exit bar approved** (two fresh clean unattended places in a row per
   type; city and early-game types by owner acceptance).
3. **Gate column: every visual row is a gate before rollout** (high quality
   is the aim). The four system rows (occupants, navmesh, interiors,
   ambience) are gates on the SOCKET, not the system: the place records
   the NPC/idle sockets, the interior promises and the ambience zone as
   data (the 16g promise vocabulary and the quest sockets already exist;
   verify and extend, never a second vocabulary), and the later phase fills
   them. No idle-occupant pass in the loop.
4. **The five Phase 11 exemplars are DROPPED explicitly** (owner: retired at
   check-in 1 on 2026-09-23; Lilmoth + 4). Delete any of their blueprint or
   plan files still in the tree, in one commit with an addendum line in
   0099 naming them. Lilmoth returns as the whole-city slice with the owner
   hands-on. The six 16i exemplars-on-paper are superseded by 16k and were
   never built; nothing to delete beyond the 16i brief's notice.
5. **The yard close-out is trimmed to what the loop needs now.** The
   workflow "16k-slice-1a" (run wf_a25dcda6-dd1) was STOPPED by the planner
   at the owner's request; resume its agents from their transcript packets
   (`python3 tooling/repo-standards/lane_resume.py --brief`) with these
   scope changes:
   - Docs reconcile stage: as briefed (items 1–9), plus rulings 1–4 above.
   - Kit-yard lane: KEEP decal flag in build_kit (the flicker; 203
     materials), the one composite yaw convention + yaw test + the render
     check actually run, the stilt-house leaf sidecar, settlement clearance
     patches with door aprons and the deck kind, the KotM huts recorded as
     promised interiors, every check-in 1–3 yard defect as a gate, republish.
     DEFER the vanilla rock cave entrance to the dungeon-entrance slice and
     the landing-stage dry-ground rule (owner ruling in
     `tooling/.reports/16h/checkin3-diagnosis.md` §7) to the first water
     village or works-and-landing slice; both go into the 16k carried
     backlog with their diagnosis pointers.
   - Miner: NO full-pool run now. The miners run per kit on demand inside a
     slice (Claywater: the Imperial farmhouse, wattle fence, signage, tent
     and clutter kits) and the abuts re-walk over the current kit pool
     (fixes the singleUse test). The full-pool run is the overnight job,
     lowest priority in the job queue, launched only when nothing else is
     queued (0099 speed rule: owner-away hours are used).
   - Combat round-7 close: keep (ten minutes; it clears a red gate every
     preflight trips over).
6. **First place: Claywater Station** (planner choice, owner not objecting):
   `place.imperial-fringe.claywater-station`. Highwater is slice 2's
   candidate. (Superseded by 0100 Consequences: Highwater is not slice 2;
   slice 2 is chosen at slice 1's close by the contrast rule.)

## Machine

Watchdog running (`bash tooling/repo-standards/cpu_watchdog.sh --status`);
every heavy job through `job_guard.sh <lane> -- <cmd>` (one slot on this
4-core box, so lanes queue: launch the kit lane before the miner). Only one
Claude session may run; the old one was killed. `/tmp` (109 GB) holds the
mod pool and caches via `tooling/bootstrap/cache-links.sh`.

## Uncommitted at the switch

`tooling/repo-standards/baseline-singletons.json` (combatAudio row removed:
the combat close commits it) and whatever the stopped workflow's docs agent
had edited (git status; its packet says what it finished). This file.
