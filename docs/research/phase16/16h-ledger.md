# 16h ledger — the building blocks (part 1 from 2026-09-22)

Measurements, Sonnet reports and departures from the
[brief](../../phases/16-foundation-and-places/16h-settlement-runtime-and-kit-qa.md).
Rows are replaced as numbers improve; the brief stays the plan.

## 1. Routing check (2026-09-22, pre-build)

Starting state held, with this drift (brief corrected in the same commit):
`packages/game-core/src/settlement/` is 9 files, 1,698 lines; cuboids are
built at `SettlementColliders.tsx:61`; `settlement-warning-known-red.json`
holds 8 rows, not 0 (lane D retires the ones the scope rule makes moot);
`vegetation-patches-receipt.json` has no `reRuns` key (the step-0 job
diagnoses the stage); no 16g "ending state" section existed (this ledger's
§1 is that). Two stale doc claims from the digest: 0075 §7 / 0073 §5 struck
the 16f deferral of rock hulls to 16h (16h inherits a working trimesh
path); `piece-front-derivation.md` "Known limit" bounds front accuracy.

## 2. Proving ground — site chosen (2026-09-22)

Three stretches measured on the shipped province (one `shared_survey`;
gates: no live record within 300 m, no route or structure within 130 m,
120 m box dry, recorded water on one edge at >= 1 m).

| | A | **B (chosen)** | C |
|---|---|---|---|
| centre m (x, z) | 3978.3, 2596.4 | **4285.4, 5727.5** | 1033.6, 5398.5 |
| studio URL | `?view=character&x=3.978&z=2.596` | **`?view=character&x=4.285&z=5.728`** | `?view=character&x=1.034&z=5.399` |
| mean / max slope (120 m box) | 2.78° / 11.61° | **4.08° / 19.66°** | 4.30° / 11.44° |
| water id / kind / level | `body.2147-1463` marsh-deep 1.15 m | **`body.2407-3123` marsh-fringe 17.93 m** | `body.611-2937` swamp 3.04 m |
| edge depth / bearing / range | 3.09 m / 203° / 83.5 m | **1.68 m / 095° / 60.6 m** | 1.02 m / 070° / 81.5 m |
| nearest live record | stilt-channel-edge-uxaneet 310 m | **lagoon-submerged-xanmeer 362 m** | umbriel-stripped-undead 309 m |
| region / soil / danger | rootland deep marsh / peat / D5 | **fringe marsh / firm lowland / D3** | interior swamp / soft / D4 |

Why B: firm lowland, the lowest danger reachable (D0–D2 exist only
around records and routes, which the gates exclude by construction; the
yard is `fixture: true` and spawns nothing), nearest the coast, a berth
edge with 2.5 m freeboard. Lore: a strip of firm coast fringe 360 m from
a xanmeer already recorded as drowned; no landing or road explains a yard
there, so an out-of-world scratch yard implies nothing.

## 3. Step 0 and lanes A–E, round 1 (2026-09-22/23)

Run as one workflow (two step-0 `run` jobs, then lanes A–E on disjoint
files; the session died twice mid-run, not from memory: cgroup peak
3.7 GiB of 12, so the lanes were resumed two at a time from their
on-disk edits).

**Step 0.** Clearance receipt: the shipped bundles ARE pruned (0
survivors in hard-clear polygons over 20 patches / 37 chunks; scratch
re-run removes 0); the stage now records `prePatchInstanceCount` per run
and keeps `reRuns`; the shipped receipt stays stale until the next
scatter run (backlog row). Remedies: root cause found, no stage applies a
meso-move to an already-committed record (`pin_overrides` skips seeded
results; the meso kind is capped at 150 m; `--resolve-all` moves
unrelated records); the island in `body.1284-3448` measured at
2341.1, 6422.9 (149 cells, ~1,991 m²); round 2 builds the `reseat`
override kind.

| Lane | Delivered | Key numbers |
|---|---|---|
| A sink + mounts | `mine_designed_sink.py`, `mine_mounts.py`, `mesh_ground_line.py`; `kit-designed-sink.json`, `kit-mounts-mined.json`; 46 manifests rewritten | 1,383 assets: plugin 296, mesh-sill 908, swap 1, policy-fallback 178; anchor ground 1,185 / deck 107 / wall 64 / water 27; 35 mount pairs from 27,286 candidates; 27 hulls with a waterline; tell vs plugin median 0.255 m |
| B runtime | −yaw in one helper; sink by `designedSinkM`; stilt exemption gone; trimesh colliders; ladder from `fx/lodFade.ts` | corners 683/733 + 3,501/4,225 wrong → 0/0 at 0.05 m; arch ray hits 0; worst resident 1,273 parts → budget 1,973; one-copy walk exactly 1 at 201 × 16 |
| C kit data | sidecars published (2.48 MB, 19 kits; flora/groundcover exempt by name); GLB header parse; test selection by directory (121 collected, 79 modules were in no suite); waiver gone; fixture refusal; schema 2 | `settlements.json` mode 0644 |
| D compile | allowlist EMPTY (0 rows); water facts 32/32 with ids; snap check in both conventions; doors bound to doorways; mounts | 6 known-red rows retired, 2 remain; 47/58 doors > 0.5 m from a doorway (2026-09-09 polygons; 16i re-authors) |
| E renderer | assembly / template / kit-sheet / mount-pair modes; grid, red ground line, front arrow, doorway dots | 48 s per template (Blender start per assembly; fixed in round 2) |

**Decisions taken from the reports (round 2 briefs carry them):**
1. Composites are baked by the kit build (`compose.parts`); the compile never expands a `composite:` ref (would ship geometry twice). Brief item 7 amended.
2. `deck` means "stands on a raised surface" (seated on a containing parent's top face, else on terrain); only `wall`/`ceiling` need a mined pair. The 58 mount errors on the five blueprints were barrels on docks.
3. The exporter reads `designedSinkM`; `buryM`/`slopeBuryPerM` are gone; part budget 1,973 by the 0052 rule.
4. `originOffsetM[0..1]` are footprint-corner offsets, not a runtime translation: brief item 3 was wrong; the sign fix alone gives 0 mismatches on 4,958 pieces.
5. The five old blueprints are re-derived on the frozen ground and published as the numeric fixture; the settlements layer is shown; nobody judges their layout.
6. Sheets are rendered `--flat` (kit truth, not terrain) in one Blender session per kit.

### Round 2 (2026-09-23; workflows `16h-part1-fixup-and-yard`, `16h-mounts-root-cause`; full lane JSON in those journals under the session's `subagents/workflows/`)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| F1 export + compile | exporter reads `designedSinkM` (fails closed on a missing one); part budget 1,973 = round(1,273 × 1.55); deck rule in the compile; the mined-offset axis swap (east, north, up → x, up, z) fixed in `mount_children`; five blueprints re-derived | only sap-tapping compiles (52 placements, 4 errors); four blueprints fail design rules rederive cannot clear → **fixture-replay mode decided** (brief § Part 1 state); `settlements.json` NOT republished; `compile_settlement --all` peaks ~4 GiB |
| F5 runtime | deck seats on parent or grounds; wall/ceiling refuse without a parent; 30 tests green, typecheck green | the layer now REFUSES the shipped schema-1 bundle: the studio's settlement layer is dark until the export republishes (step 1 of the resume list); `mountOffsetM` missing on deck children in the compile output |
| F6 proving ground | `world/sources/blueprints/place.fixture.proving-ground.json` authored at the §2 site, compiled | laid out BEFORE the owner's <2° rule (relayout is step 2); no hull `designedWaterlineM` on the chosen hull; mount pairs were the old snap set; 3 asset refs lack a policy row (test red until relayout) |
| F2 reseat | `reseat` override kind in `plot_remedies.py`/`apply_sitings.py` (7 tests); Blackrose centre moved onto its island; three wet villages and raft-village re-seated; catalogue republished | Blackrose's blueprint/layout is stale against the new centre (16i's city pass); minor routes/waterways not re-derived to the moved dots |
| F3 renderer | one Blender batch per kit set, `--flat`, `--skip-existing`, `--all-mount-pairs`; 48 s → 4.08 s per template; all 377 templates in 1,539 s; peak 3.75 GiB | round-1 non-flat dirs superseded (delete) |
| F4 kits | underwater-v1 rebuilt (0 identical rock levels, 79/79 with sink); waterfall-fx-v1 sidecars published | waterfall-fx-v1 deliberately not compressed (FX kit; reason in its report) |
| G mounts | anchorClass by placement evidence, decided once in `mine_mounts.py`; manifests: ground 1,335 / water 27 / deck 12 / wall 9 / ceiling 0 on 1,383 assets; evidence plugin 571, mesh-tell 812 | **0 real mount pairs**; vanilla hangs lights on walls (sconce n=337) and signs on posts (100 %), never a lantern on a post: yard exemplars re-decided (brief resume step 2); interior references must count for containment; the "mudmother lanterns' parents are not in our kits" claim was WRONG (research 2026-09-23): the three 6.4 m lanterns hang in the Mud Mother Hist tree (`mudmother:gv_meshes/argoniannest/histtree`, already in settlement-mud-v1), which `mine_mounts.py` dropped because the parent ref is scaled 0.77 and the miner admits unit-scale refs only (line 216, `SCALE_TOLERANCE`); round 3 fixes the miner (scaled parents admitted, local offsets divided by the parent scale) |

### Round 3 (2026-09-23; workflow `16h-part1-round3`, lanes H/I/J then K then R)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| H fixture replay | `compile_settlement --fixture-replay` (every hard finding and warning into `fixtureWaived {ruleId, grade, message}`, doc `fixtureReplay: true`, printed `FIXTURE-WAIVED`); exporter treats `fixtureReplay` like `fixture`, refuses without `--fixtures-ok`, re-derived findings pass only when their exact message is in the receipt; `test_fixture_replay.py` (floats > 0.3 m, sills 0.15 m, replay flag); round-1 non-flat sheet dirs deleted (220 files, 65 MB) | five compiles clean under replay: mazzatun 100, nine-trunks 85, wamasu-pond 27, lilmoth 466, sap-tapping 52 = **730** placements; **existing defect found and fixed**: `build_bundle` re-derived compiledObjects without kit interiors/connectors/doorways so every doc failed the match gate (47/97, 54/113, 10/42, 77/239, 16/51 objects differed); sap-tapping too needs replay (3 network-stitch + 1 door threshold); `mountOffsetM` on deck children was already there (F1): the "missing" field was the stale schema-1 bundle; export still blocked (round 4) |
| I renderer | orthographic straight-on elevations (front/back/left/right by the front arrow), PIL scale bar (1 m ticks, labels every 5 m) + asset/view label, 3 px cut line with white halo (clamped with a label off-frame), magenta child / 40 % ghost parent, 9 tests red first (20 green) | template 3.6 s, mount pair 6.4 s per 6 frames at 512 px; plan frame not annotated; no mined pair existed to render (hand fixture) |
| J kits + miner | settlement-imperial-v1 24 → 29 assets (sconce, free wall, sign post, huntsman + stables signs), rebuilt, compressed, published (7.67 → 10.34 MB); miner admits scaled parents (offset ÷ parent scale) and interior cells; policy rows for the 3 yard refs + 5 new assets | **still 0 pairs** on 159,557 candidates: the gate wants one offset (spread < 0.3 m) but sconces sit anywhere along a wall (impfreewall01 n 19, spread 3.67 m) and lanterns on three branches (n 3, spread 8.4 m); sconce containedShare 0.505 (< 0.6) and sink IQR 0.60 kept it off `wall`; `argonianlanterns03` containedShare 1.0 → wall; `mine_designed_sink` needs a 6 GiB ceiling; `shipcyrodiilicrope01.dds` missing on the ferry build. Round 4 makes pairs bands/point sets |
| K yard | all 10 parcels moved to < 2° at their centres; compile 12 errors → 0 (7 warnings); hull 22.5 m east onto water | footprint EDGES still 2.26–4.66° on gate/wall/tower/stilt-hut/cave; exporter refused the yard's non-flood warnings; 97 C6 (7.2/ha) from the spread; round 4 re-lays footprint-wide within 250 m |
| R re-render | none | memwatch 4 GiB ceiling below the cgroup's 4.4 GiB baseline (other sessions hold ~2.2 GiB): use 7 GiB, one process at a time |

**Decisions taken (round 4 briefs carry them):** a mount pair is a `band` (out/up tight, along free on the parent face) or a `points` set (discrete offsets, e.g. lantern branches); containment counts any static ref; the sink-IQR gate is for ground classes only; a `fixture: true` compile moves WARN findings to `fixtureWaived` on its own (errors stay fatal, the yard is never replay); a mesh whose decimation collapses still publishes a 3-tier ladder sharing one primitive; owner rule 1 is footprint-wide.

### Round 4 (2026-09-23; workflow `16h-part1-round4`, M ∥ E, then K2, then R2)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| M miner | pairs as `band` / `points` (schema 2, `pairKindCounts`, both keep a representative `offsetM`/`yawDeg` so the compile reads them unchanged); any STAT/TREE ref counts for containment; no sink-IQR gate on wall/ceiling | 504 pairs (25 band, 479 points) on 84 children; sconce containedShare 0.505 → 0.979, class wall, band on impfreewall01 (n 19, out 0.15 m, up 2.74 m, along −3.0..3.1 m); lanterns03 → histtree 3 points (parentScale 0.77). **Over-wide:** wall 12 → 60 (chairs, salt piles, FX, Ayleid stones: room shells enclose everything inside them), TREE shrubs 79 junk pairs, deck 15 → 36 via non-kit statics, signs (n=1) never class; `placement_metadata.load_anchors` refuses schema 2 → round 5 (face contact, thin evidence, v2 reader) |
| E export | `plan_lod_levels` (build_kit + Blender half, identical by test): a level identical to an earlier one `sharesLevel` → one mesh, full 3-tier ladder; fixture compiles waive WARN findings on their own; `mwimparchwall01` pad compatibility row; ferry ropes/sails: SBOT 1.2 omits the diffuse, 1.1 fetched with the Nexus key into the vault (credit line: SBOT 1.1) | underwater-v1 25.6 → 13.5 MB, imperial 23.0 → 10.3 MB (republished); export tests 59/61: collider budget 1600 ≠ 1973 (stale shipped data, clears on publish), farmhouse composite authored `pad` vs manifest `stilt` (yard uses the manifest, round 5); 25 underwater / 41 flora / 78 groundcover / 26 waterfall-fx short assets keep the old skip until rebuilt |
| K2 yard | relaid footprint-wide within 250 m; sconce-wall and sign-post parcels added; hull 28 m E, 8 m N over >= 1.2 m water; footprints/districts/routes re-derived | gate+wall 1.57°, tower 1.99°, mud hut 1.68°, **imperial house 2.002°** (no spot < 2° within 250 m under the gates), **stilt hut 2.64°** (its < 2° spots cut the door path); compile 1 error: the site record `world/sources/sites/proving-ground.json` never existed on disk (the round-2 claim was wrong); way to the hull fords 19.6 m (limit 12 m) → dock piece; mounts not placed (schema 2 reader); one prose hit |
| R2 render | templates-flat complete, 2,262 PNGs / 377 assemblies in 28 min, peak 5.45 GiB; mounts 1,370 PNGs at cut-off (superseded by the re-mine) | gate and the ten kit sheets not reached (budget); round 5 renders them |

**Decisions (round 5 briefs):** a STAT parent counts for wall/ceiling only by FACE contact (±0.35 m of a vertical or bottom OBND face); TREE parents keep volume containment but only for wall-class children; deck on kit parents only; n < 3 with share 1.0 classes as "thin" evidence (the once-placed signs); a direct/pad fit sits on < 2° cells, a stilt fit up to 3° because it stands on legs; the way reaches the hull over a docks-v1 jetty, never a ford.

### Round 5 (2026-09-23; workflow `16h-part1-round5`, M2 → K3 → R3, after the tooling lane's step B)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| M2 miner | face contact for STAT parents (±0.35 m, 0.15 m margin), TREE volume for wall children, deck on kit parents, thin evidence (n < 3, share 1.0), `placement_metadata` reads schema 2, cell streaming (peak 1,982 → 1,613 MiB, byte-identical) | ground 1,299 / water 27 / deck 15 / **wall 46** / ceiling 1; 16 band + 229 points; sconce → impfreewall01 points n 21 (up 2.74 m, along −2.98..3.36); **still noisy**: chairs, benches, basket03, mine planks, waterfall FX are "wall" (the bottom-face band catches floors), the huntsman sign is ground (10 % volume gate), lanterns03 ground (TREE hits never added), the post across a cell border unseen. **Regression:** the manifest writer rewrote `designedSinkM` on 64 assets (0.25 → −6.23 m). Round 6 fixes both |
| K3 yard | site record `world/sources/sites/proving-ground.json` authored (fixture path accepts it); slope limits by fit (direct/pad < 2°, stilt < 3°); dock `composite:docks/quay-run-2` (14.9 m) so the way reaches the hull over 15.7 m of deck; sconce mounted (offset −1.50, 2.70, 0.16, yaw 270°); hull halo min depth 1.32 m; `test_proving_ground.py` 8 checks | 72 placements; compile 1 error (stilt hut door at 180° from its 17° path), landing stage 4.36° > 3° (a dock piece: exempt in round 6), sign unplaced (no pair); **settlements.json still schema 1, unpublished** |
| R3 render | mounts (M2's 245 pairs, superseded by round 6's re-mine), gate, ten kit sheets at `--jobs 4` | 6,373 PNGs, 0 failures: templates-flat 2,262, mounts 1,470 (24 min, peak 4.4 GiB cgroup), ten kit dirs 635 (< 10 min each), gate 6; both pair kinds rendered. Sonnet protocol tuned on a 14-sheet sample first (rule 8), then one sweep |
| K4 yard | dock slope exemption: a piece held by a quay kit's published manifest (`DOCK_KITS` = `docks-v1` in `test_proving_ground.py`) carries no building slope rule; new test holds the landing stage passing at 4.36° and the direct sconce wall and pad mud hut failing on it. The compile has no degree slope check of its own; the fit limits live only in that test | 72 placements, 1 error, 0 warnings (9 fixture-waived). Slopes: gate 1.73, wall 1.12, tower 1.69, imperial house 1.64, stilt hut 2.94, stair 1.31, mud hut 1.83, cave 1.57, boardwalk 2.78, landing stage 4.36 (exempt), hull 2.31, sconce wall 1.51, sign post 1.89°. **Stilt hut door not fixed by yaw:** the threshold-to-doorway gap is 12.73 m at every yaw tried (0–332°), because the blueprint casts the threshold from the entrance's `sideDeg` 142.5 while its `offsetM` [0.58, −2.82] and the compile's open-front doorway [−0.07, −2.77] (`sideDeg` 225) point the other way. **Sign not placed:** `kit-mounts-mined.json` (14:36 write, schema 3, 66 pairs) holds no `signwrdrunkenhuntsman01` pair and the manifests class it `ground`. Export not run (compile red), so settlements.json stays schema 1 and unpublished; yard tests 8/11 (3 need the publish), export 60/61 (collider budget), placement suite 39 red, 4 of them `test_place_obligations` refusing the fixture as an unexpected place |
| K5 yard | one measured opening per entrance: `interiors_index.doorways_retry_off_centre` re-reads `sideDeg` as the bearing of `offsetM` from the plan centre (new `planCentreM` on every probed record); all 23 kits re-derived; `piece_doorways` binds to the canonical `entrance` first; `threshold_uv` sites a fixed entrance on its `offsetM` (outline cast only when no offset); slope rule moved into the compile as `97 B3` (ERROR; `docks-v1` exempt by manifest kit; `fit_slope_failure` in `compile_settlement`, the test copy deleted); obligations skip fixtures via `blueprint.is_fixture` (the exporter's predicate, moved) | entrances changed 3 (stilt house + its composite 142.5 → 0.13°, `1mjyaztecbuilding1` 135 → 192.7°), provenance rows re-sided 4, offsets unchanged; stilt hut re-oriented 234.7 → 17.1° (`--orient --apply`, `--areas`, `street_router --apply`). Projection dropped on the planner's call (the stilt opening lies 3.9 m inside the derived outline, behind the veranda): yard 72 placements, **0 errors**, 9 fixture-waived. Lilmoth's 4 stilt houses re-oriented (council hall 342.3 → 145.9°, pus tower 8.0 → 141.6°, spine tavern 171.6 → 317.6°, bench guild hall 206.9 → 350.4°; `orientationWhy` rewritten); replay 104 → 101 waived: 8 facing + 4 threshold + 3 old `97 B3` gone, 3 new `97 B3` (10.4–14.95°), 4 `door-on-way` + 4 `integration` door-to-way (4.0–11.4 m) and 1 boardwalk-crosses-parcel (pus tower) added. 1mjyaztecbuilding1 entrance re-derived to 192.7° by the fallback probe; unplaced today; the kit-sheet sweep judges it. Replays clean (0 errors each); new `97 B3` waived rows: Mazzatun 10, Nine Trunks 13, Sap-Tapping 7, Lilmoth 49, Wamasu 1 (80: stilt 50, pad 27, direct 3; 2.44–44.79°); Lilmoth's 4 stilt-house doors now also waive facing (8) and threshold 12.46 m (4); the entrance bind cleared 13 mudhut01/kiosk door waivers. Tests (proving ground, replay, obligations, blueprint, compile, export, interior promises, interiors): 326 passed, 4 failed (3 need the publish: sconce, sign, bundle sill; 1 collider budget, red since K4) |
| Test-fix | placement reds grouped by cause, fixed once each: (1) `plot_remedies` tests still expected a pin/merge to clear a PLOTTED record's dot, reversed on 2026-09-20 (561ebb19): tests now cover both branches (8); (2) `compile_minor_waterways` stamped a writeable `_centre_depth_grid` on the shared survey: now `ProvinceSurvey.centre_depth_grid`, frozen (survey_cache 2); (3) `apply_sitings` wrote `plotFacts` without the `water` graph-id fact (reseats and meso pins): both writers use `remeasure_plot_facts.measure`; 9 records re-measured, positions byte-identical 827/827; places.json re-exported (water provenance 3); (4) ce71aee7's prose pass rewrote sources without re-exporting: `export_routes` re-run (3); the Uxa-Meen `CAST_JOIN` key follows the record's slot text (roster 2); (5) a `fixture: true` site record counts as a known place in the dangling-ref gate (1) | placement suite 23 red / 2,765 passed / 9 skipped (249 s, one process), then `test_export_places` green after the re-export: 22 red. Left, with owners: Blackrose reseat without its derived `cityLayout`/footprint (macro_plot 2, catalogue footprint 1); NPC ids derived from slot prose, so ce71aee7's two role rewrites renamed published people (roster 3); stilt-village band 6–9 vs 5 live since the 16g cut (`rederive_count_bands`); road condition (16f bake, unchanged code); attachments (flora kit); collider budget + fixture replay + yard (publish, K5); mine_mounts 5, blueprint live-dir/export (K5/miner) |
| Test-fix r2 | (1) Blackrose: `macro_plot.city_centres` takes a reseat row's point as the city centre; `apply_sitings.seat_record` moves `cityLayout.centre` to it and re-derives the way and footprint polygon with `city_layout` (16 vertices, source polygon); (2) `notableNpcSlots` are `{slotId, role}`; one-time migration set slotId from the published person id (496) or minted it (56: 54 on cut/deferred places, 2 cast principals); `_slot_ids` and `CAST_JOIN` (46 keys) key on slotId; tests for the slotId, a reworded role and a missing id; (3) type bands: `stilt-village` 5–9 and `reef` 2–6 (the 2026-09-20 cut: Longmont, Keel-Sakka Stilts, Whitebone Reef, Mangrove Reef), 0041 addendum; (4) road-wear paint cut (owner 2026-09-23): condition branches out of `landcover.compile_ground_control`, three paint tests removed, 16f ledger note, stale backlog row removed; (5) attachments: red with the HEAD kit.json too | macro_plot 2/2 green, positions 562/567 identical to HEAD (the 5 differing are the reseat rows); footprint gate red: 9 Blackrose satellites inside the 230 m band, satellite reseats held (rule gives 285–825 m moves, water records); roster 0 renames / 0 name changes of 498, translated cap green; type_recipes 9/9; landcover+road 32/32; attachments 189 / 2 / 447 identically on HEAD and tree kit. Placement suite (one serial run, 240 s): 7 red / 2,785 passed / 9 skipped: attachments 1, catalogue footprint 1, export_blueprints 1, collider budget 1, fixture replay 1, proving-ground mounts 2 |
| Test-fix r3 | `macro_plot.footprint_clearance`, one function used by both `separation_ok` and `typed_siting_violations` (the test's gate): a record with `footprintSource: polygon` is measured dot-to-polygon-edge against the other record's own radius; the radius-sum rule stays when there is no polygon; related pairs that may abut keep their dot-to-dot rule. `apply_sitings.reseat_points_m` is the one reseat lookup, read by `city_centres` and `city_layout.solve` (a reseated city is scored at its point, the lattice is not searched). `export_blueprints` re-run | export_blueprints, macro_plot, city_layout green; catalogue footprint red on 10 pairs: 9 Blackrose satellites (8 inside the polygon at 0 m, natural-dive-shaft 7.8 m of 30) because the derived polygon is the hull of dry cells across the whole 230 m disc (16.1 ha of 16.6 ha), not the island; plus air-pocket-station vs drowning-narrows 72.8/75 m. No record moved |
| Test-fix r4 | `city_layout.footprint_polygon` hulls only the dry component connected to the centre cell (4-neighbour); `city_layout` re-applied (5 cities' polygons changed), `--reseat`, `export_places`, `export_blueprints` re-run. Air-pocket nudge NOT applied: 3 m further along the bearing is water (all 1–39 m out), and the record is a land dot, so `check_reseat` would refuse it | polygons: Blackrose 16.1 → 0.17 ha, Stormhold 15.86 → 11.71, Thorn 15.94 → 8.76, Gideon 15.67 → 14.84, Alten Corimont 5.87 → 3.45. Blackrose satellites clear except manned-toll-tower (8.1 m of 45). Named tests 96/97 (catalogue footprint: toll tower + air-pocket 72.8/75). Placement suite 5 red / 2,792 passed / 9 skipped |
| Test-fix r5 | Owner 2026-09-23: Blackrose spreads from its island over the lake and onto the shore. Round-4 component polygon reverted; `city_layout.city_footprint`: a reseated city whose centre's dry component is under 1 ha keeps the 230 m disc (`footprintSource: band`, since the validator has no `radius`; `footprintWhy` added and classified in `place_obligations`). Abutters via `sitingPrefs.boundTo` (with `why`): drowned-hist, lake-divers-yard, natural-dive-shaft, manned-toll-tower → Blackrose (250 m); air-pocket-station-deeps → drowning-narrows-tidal-gate (100 m). Land satellites reseated (plot-remedies + override rows, ±60° search, clear of every other footprint) | Stormhold/Thorn/Gideon/Alten Corimont polygons byte-identical to HEAD (15.86/15.94/15.67/5.87 ha). Moves: cordon-cellars 155.0 m (369.5 m from centre), necromantic-dig 62.9 (275.5), rose-bone-waystation 168.7 (275.5), rose-outworks 138.3 (270.5), saltrice-village 72.5 (295.5); reseat 10/0 refused. Residual footprint red: toll tower 33.4 m vs the 45 m abut floor. Placement suite 9 red / 2,788 passed; city_layout + obligations then fixed (44/44), leaving 7: attachments, catalogue (toll tower), collider budget, fixture replay, mine_mounts 2 (miner lane), proving-ground sign |
| Test-fix r6 | Toll-tower move to 45 m out along the Blackrose way (centre → gate bearing) NOT written: 45–50 m out on that line is lake water (`water_at` wet, dry_grid false) and the tower is a land record, so `check_reseat` refuses it | pair unchanged at 33.4 m vs 45 m; no files changed this round |
| Test-fix r7 | Toll tower reseated to the first dry cell on route.road.soulrest-blackrose past 45 m from the Blackrose centre that also clears the drowned Hist's footprint (the first dry cell, 77.4 m, stood 72.2 m from the Hist against 85 m); receipt keeps the original fromM | 2267.4, 6377.9: 89.0 m from the centre, 108.3 m move; footprint gate clean (0 pairs); catalogue + macro_plot + city_layout + export_places + export_blueprints 97/97 |

**Decisions (K5, 2026-09-23):** the threshold is the measured opening, and the outline cast is used only when no measurement exists; the footing slope rule is a compile rule (`97 B3`, ERROR, `docks-v1` exempt); fixtures are skipped by the place obligations through the catalogue/exporter predicate `blueprint.is_fixture`.

**Root cause found (round 6, owner 2026-09-23):** the miner never used the real meshes. Bounding boxes were the evidence, and a box round a chair overlaps a box round a wall; every round since added rules on top. With the plugin's exact position, rotation and scale and the actual mesh surfaces, attachment is a distance check (<= 0.03 m) and the touching side of the child decides the class (behind/beside = wall, top = hanging, bottom = standing). No classification rules; boxes only filter candidates. A `hanging` class (arm, crown, ceiling) replaces `ceiling`. Decision 0085 §1 amended.

### Round 6–13 miner (2026-09-23; lanes M5–M13; every batch and run in `tooling/world-generation/worldgen/fixtures/mount-golden.json`)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| M5/M6 miner | real mesh-to-mesh contact (`mine_mounts.py`, schema 3): 300 seeded child samples, 0.03 m, support from below wins; NIF mesh cache via two Blender dumpers; golden set of 25 with expectations written first, 40 refs/asset (seed 20260923) | golden runs 1–3: 17, 20, 22 of 25; held-out batch 0: 22 of 25 (door rule fixed after the run: any frame contact counts) |
| M7 miner | round 7 rules: (a) an interior shell (`architecture`/`dungeon-kit`/`ruin`) held only by other kit shells is ground, `support: interior-zero`; (b) a pivot within 0.10 m of a placed water surface (base model under `meshes/water/`, own or master record, pivot inside its bounds) is water; (c) `rtbanneranchor01` out (kept under `golden.left`), `arpitretractablestairsbase02` in (seed 20260924, unplaced); unit tests for the 0.10 m band (cell water, placed water) and for (a) | **golden run 4: 23 of 25**, 99 s, 4,012 poses, 0 meshes missing, cgroup peak 6.35 GiB. Fails: `gkblillipad2` (ground 30 / water 10) and `cedartree3` (hanging 19 / ground 12 / deck 5 / wall 2 / water 2). Rule (b) matched no reference of either: the 57 placed-water forms are all Skyrim.esm planes and none lies under them. Lily pads: pivot − cell water p50 −0.77 m, pivot − LAND p50 −0.35 m. Cedars: pivot − cell water p50 −1.51 m, pivot − LAND p5/p50/p95 −4.42/9.66/29.34 m. Held-out batch 1 and the full run not started (protocol stop); `kit-mounts-mined.json` stays INTERIM |
| M8 miner | round 8 rules: an exterior reference with no usable terrain (LAND missing, an unresolved flat placeholder, or the pivot more than 2 m below the LAND) is dropped and counted per asset (`droppedNoTerrain`), replacing the M6 TREE and side-only rules; a Skyrim.esm city worldspace (WhiterunWorld, ...) reads its parent's LAND at the same grid; a drop leaving n < 3 falls back to the sink waterline (`evidence: sink-waterline`) else the placement-policy row (`water-zero` water, else ground; `evidence: policy`); interior-zero counts a copy of the same asset as a shell contact and ignores kit clutter contacts; tests for each. Huntsman sign n 2 is one Skyrim.esm ref touching two posts (offsets y +0.64 / −0.69 m), not a duplicate: no dedupe | **golden run 5: 23 of 25**, 103 s, 4,012 poses, 0 meshes missing, 47 of the sampled refs dropped, process-tree peak 4.0 GiB. Fails: `gkblillipad2` (37 usable of 40: ground 27 / water 10; `is_water` rejects on waterline IQR 1.03 m ≥ 0.5 m, the pivots sitting −1.25..−0.28 m under the cell water, p25..p75) and `cedartree3` (15 of 40 dropped; 25 usable: hanging 11 / ground 8 / deck 4 / wall 2; over all 260 real-LAND refs pivot − LAND p25/p50/p75 is −0.66/+9.66/+26.4 m, so the drop rule, which only catches pivots under the LAND, keeps them). Held-out batch 1 and the full run not started (protocol stop); `kit-mounts-mined.json` stays INTERIM |
| M9 miner | round 9 rules: bases resolve through each plugin's on-disk masters (`FormResolver`, load order vanilla → .esm → .esp); exterior cells merged over the load order (last real LAND, last CELL water, reference overrides replace; a flat placeholder never displaces real LAND); water column (cell water above the LAND, else placeholder or worldspace default land, at the pivot) = `water` unless mesh contact from below, replacing the 0.10 m cell-water band; `droppedNoTerrain` → `droppedBuried`; `apply_placement_metadata` omits `designedWaterlineM` for a water class with no mined waterline (was a KeyError); 5 tests | **golden run 6: 21 of 25**, 157 s, 4,120 poses, 0 meshes missing, 36 refs dropped, process-tree peak 5.28 GiB (cgroup 5.94 GiB). Refs mined for the golden 25 (before sampling) 12,501, of which 3,243 have a base another file defines (this includes later plugins' overrides of a master's refs; the other 9,258 are what the old reader could see). 208 sampled refs in a water column; water displaced ground 90 / hanging 37 / free 35 / wall 15. Fails: `bridge01` water 28 / ground 9 / deck 3 and `stilthouseext` water 2 (ends and stilts on the terrain under the water: terrain contact does not count in a water column); `wrfencestr01` ground 33 / wall 5 / deck 2 (Valenwood.esp places the vanilla rail 59 times, every one touching the terrain; Skyrim.esm alone is wall 15 / deck 13 / ground 10); `signwrdrunkenhuntsman01` dropped (a WRLD override by a later plugin hid the Whiterun city's parent LAND; fixed after the run, the sign now reads the parent LAND, re-run not done). `gkblillipad2` water 40, `cedartree3` water 29 of 36. Held-out batch 1 and the full run not started (protocol stop); `kit-mounts-mined.json` stays INTERIM |
| M10 miner | round 10 rules: contacts decide first everywhere (support from below including terrain, then wall/hanging); only a reference with no contact is classed by the water column (water inside, free outside); the vote is the defining file's usable refs, other files' only while it has < 3 (`otherFileRefsNotVoting`; pairs from every file); interiors merged over the load order; `LoadOrderIndex` shared in `mine_designed_sink.py`, which now reads master bases and leaves out refs standing on a static or kit mesh (`static_supported_refs`; fewer than 3 left = `mesh-sill (plugin refs static-supported)`); contact workers start from a forkserver, one chunk each (a fork copied the 1.5 GB collect heap into every worker: 10 GiB, killed twice) | **Sink run** (once completed; two earlier attempts killed by memwatch in the shared cgroup, record untouched): 912 s, cgroup peak 6.86 GiB, refs joined 52,347 → 59,261 (29,253 through a master's base), 4,981 static-supported refs left out, 10 static-supported mesh-sill fallbacks (2 flagged with no mesh tell); p50 changed on 182 assets (+1 new, 2 lost), median abs change 0.069 m, max 202.30 m (`passcirc45r1280h0ldi01` −203.81 → −1.52). `totem03` unchanged −3.48 (0 of 11 refs found on a mesh); `scaffoldbase3sided01` −2.05 → −2.10 (6 left out, 5 remain). **Golden run 7: 20 of 25**, 180 s, 4,119 poses, 0 meshes missing, cgroup peak 3.81 GiB; refs 12,501 (defining file 9,434, other files 3,067; 165 not voting). Now pass: `bridge01` ground 28 / deck 2, `stilthouseext` ground 2, huntsman sign hanging (pair n 2). Fail: `gkblillipad2` ground 19 / water 1 (stems on the bed; sink waterline IQR 0.98 m); `cedartree3` hanging 21 (crowns on cypresses, pair to `cypress1` n 52); `imperialship01base` deck (sink waterline IQR 0.49 → 0.96 m with a master-base ref); `dockstrsol01` ground 10 (waterline n 17 → 88, IQR 0.21 → 3.17 m); `wrfencestr01` ground 7 / wall 5 / deck 2 (the 40-ref sample holds 14 Skyrim.esm refs). Held-out batch 1, full run and manifest refresh not started (protocol stop); published manifests now disagree with the new sink record on 230 assets (`test_every_published_manifest_sink_equals_the_record` red until the refresh) |
| M11 miner | round 11 (last rule change): terrain contact on SUBMERGED ground (below the cell or placed water surface at the contact) = water, dry terrain = ground, a kit/static underneath stays deck/ground, support from below still beats side/top; golden/batch sample takes the defining file's refs first; sink waterline from the defining file's refs when it has 3 or more; expectations `stilthouseext` water, `wrfencestr01` wall set before the run. `totem03`: its 11 exterior refs touch cached meshes (xanmeer `1mjyaztecbuilding5`, `3mjyunderwaterruins8`, cliffs); the one missing NIF (`wrintfloorstmid01largealtar01`, vanilla) is an interior neighbour, so the missing-mesh "unknown" rule was not added | **Sink run**: 887 s, cgroup peak 6.28 GiB; sink p50 identical to round 10 (182 changed vs the pre-round-9 record, median abs 0.069 m, max 202.30 m); 53 waterlines changed. **Golden run 8: 22 of 25**, 178 s, 4,517 poses, 0 meshes missing, cgroup peak 3.15 GiB; 33 other-file refs not voting. Now pass: `gkblillipad2` water 35 / ground 4 / wall 1, `wrfencestr01` wall 15 / deck 13 / ground 10 (band on `wrfencebasestr01` n 17), `dockstrsol01` water (waterline IQR 0.21 m, defining file). Fail: `stilthouseext` ground 2 (thin: its two stilt contacts touch terrain at or above the water); `cedartree3` hanging 21 / water 6 / deck 4 / ground 3 / wall 2 (the crowns touch cypresses and the trunks do not reach the bed 29 m down); `imperialship01base` deck 9 / hanging 9 / water 2 / wall 1 (hull parts rest on each other; waterline IQR 0.96 m). Protocol stop: batch 1, full run and manifest refresh not run; the published manifests still disagree with the sink record on 230 assets |
| M12 miner | planner ruling: the residue is a design fact. `water-zero` asset rows for `imperialship01base`, `cedartree3`, `stilthouseext` in `placement-policies.json`, each with its why in the new `assetPolicyEvidence` map; an AMBIGUOUS vote (1 <= n < 3, or winning share < 0.6; `is_ambiguous`) whose policy row names a class takes it (`evidence: policy`, `votedClass`). `totem03`: its mesh bottom is 0.006 m ABOVE its pivot (bounds z 0.006..1.569 m), so the plugin sink −3.48 m (pivot 3.48 m above the LAND) is not a ground line: the totem stands on something the contact test did not see | **Golden run 9: 25 of 25**, 178 s, cgroup peak 3.48 GiB. **Held-out batch 1** (seed 20260925, expectations written first): **20 of 25**, cgroup peak 2.48 GiB. Fails: `imphall4way01` deck 14 / ground 12 (the interior shell rule did not hold it); `kelptallstatic01` wall 9 / hanging 4 / free 2 / ground 1 (kelp touching rocks in the water: contacts first); `tel_int_connector_01` ground 17 / wall 10 / hanging 8 / deck 1; `histroots06` and `mineoredepleted01` unplaced ground (expected wall: my expectation was wrong, neither is placed). Protocol stop: full run and manifest refresh not run; the 230-asset manifest/sink disagreement stands |
| M13 miner | **Found on disk** (previous agent, cut off, no rows): code for decisions 1, 2, 4 and the stilt deck datum (`mine_mounts.py` 20:24, `mesh_ground_line.py` 20:28, `mine_designed_sink.py` 20:29, tests 20:36, policies 20:25); batch-2 expectations (seed 20260927) in `mount-golden.json` but never scored; a FULL sink run (record 20:54) and a FULL mounts run (record 21:14) made before batch 2 was scored; no refresh. **Added:** decision 3 (a `deck` policy, `tel_int_connector_01` row + why, `deck` anchor-only in `mesh_ground_line.ANCHOR_ONLY_POLICIES`; the kit holds no other connector), its test; the anchor-evidence test admits `category` (fx rows). Stilt sample (expectations written after the predecessor's run, checked against the deck band measured independently): `stilthouseext` and `stilthouse-with-door` −8.47 → −1.62 (deck −1.27, 72 % of footprint), `dockstr4way01`/`dockstrent03`/`04`/`dockstrsol02` −0.35 (deck 0.00), `quay-run-2` −6.30 → −0.31, `tamu_wooddock01` −0.07 → +1.16 (deck 1.51); `dockstrsol01`/`dockstrent01`/`02` keep plugin p50 (−1.79/−1.06/−0.04, dry-ground refs). **`farmhouse01-with-door` fails:** stilt row, but no deck on legs (largest low up-facing band z −0.02 m, 3.8 % of footprint; the exterior has no floor), so tell door-sill −1.82 (mesh bottom); its base `farmhouse01` has plugin p50 −0.05 (n 20) with identical bounds and origin | Sink record re-derived with `--complete-only` from the final code: byte-identical. 59,261 refs joined, 3,872 plugin-unsupported left out (24 assets fall back to `mesh-sill (plugin-unsupported)`), 4,981 static-supported, 60 deck-top tells; published manifests disagree with the record on 277 assets (plugin 186, deck-top 60, floor-plane 25, post-foot 4, other 2). Full mounts record (predecessor, pre-row): ground 1,208 / water 53 / hanging 45 / deck 36 / wall 25 / fx 21; fallbacks policy 13, sink-waterline 2. **Batch 2** (seed 20260927): 19 of 25, 116 s, 0 meshes missing, peak 3.97 GiB; all 6 misses are expectation errors (5 never placed: 0 exterior refs; `stockadewallwalkway01` stands on its posts). `tel_int_connector_01` → deck (policy). **Batch 3** (seed 20260929): 23 of 25, 124 s, peak 4.15 GiB; `shackframelmid01` expectation error; **`rockcliff07` real**: 37 of 40 sampled refs `droppedBuried`, the 3 left tie and go to wall; in the full record `rockcliff04` is water 21 / ground 20 with 330 buried. Protocol stop on a rule question (buried refs dropped, not counted as ground): full run and refresh NOT run. Tests: `test_mine_mounts` + `test_mine_designed_sink` 38 passed / 2 red (the two manifest-vs-record tests, cleared by the refresh); `test_placement_metadata` 18 passed / 1 red (`expanded` set empty: no compiled settlement expands `dungeon-root-v1`, other lane's compile output) |
| M14 miner | planner rulings 2026-09-23. (1) buried is ground: a pivot > 2 m under the LAND votes `ground` (`buriedGround`); only no-LAND refs drop (`droppedNoLand`, was `droppedBuried`) (`mine_mounts.py:539`, `:1107`). (2) a vanilla static under `meshes/dungeons/` is a shell in both interior rules (`is_vanilla_shell` `:186`, `:715`, `:1116`–`:1131`). (3) `farmhouse01-with-door` row stilt → plinth (the coverage test needs an asset row); `complete_record` gives a composite whose `sizeM`/`originOffsetM` equal its `compose.parts[0]`'s the base's plugin sink, evidence `base:<id>` (`mine_designed_sink.py:546`–`:620`; `base:` in `SINK_EVIDENCE_PREFIXES`). (4) sconce trace (round-13 code, every ref): sconce joined 298 (293 interior), static-supported 0, buried 0, voting 283, wall 254, 14 mount rows on `impfreewall01`; the pair died at the parent filter (`is_mount_parent`: ground/deck only) because `impfreewall01` voted wall 164 / ground 95 (share 0.594): its side contacts were vanilla floor and pillar statics, not shells, so rule (a) failed. No round-13 rule removed it; ruling 2 is the root fix. Tests: burial, vanilla shell, composite base sink, farmhouse row | After: `impfreewall01` ground 222/276 interior-zero, sconce pair back (band n 14, offset 0.08/0.16/2.72 m); `improomdoor01` ground 36/37 and `imphall4way01` 25/26, both interior-zero. **Golden run 10: 25/25**, 175 s. **Batch 5** (seed 20260931, expectations written first): **22/25**, 53 s, cgroup peak 4.08 GiB (about 3 GiB of it other lanes). Misses: `imperialship01masts` water (sink waterline, water-first stays: my expectation was wrong); `kioskbarrierei01` hanging 40/40 (its up faces meet `shipkatariahwoodfloor01`, the walkway plank laid over it, on all 122 refs); **`commoncounter01` wall 4 / hanging 2 / deck 1: real**. No floor is seen under any of its 9 refs, and a top contact with the fish, bows and candle lying ON it reads as hanging. Protocol stop on a rule question: the full mounts run and the manifest refresh were NOT run. Sink record `--complete-only`: 5 rows changed (farmhouse01-with-door −1.82 → −0.05, farmhouse02-with-door −1.82 → +0.13, bamboohut01/02-with-door +3.03 → −0.09/−0.11, all `base:`; mud hut-with-entrance −2.94 → −3.02 from the rebuilt raw tell). Tests: `test_mine_mounts` + `test_mine_designed_sink` 41 passed / 2 red (manifest vs record, 344 rows, cleared by the refresh); `test_placement_metadata` 17 passed / 2 red: the repository coverage test (`expanded` empty: needs compiled output) and the shipped-kit contract (`windchimehavok` water, n 1, no waterline, in `settlement-mud-v1`, twice) |
| M15 miner | planner rulings 2026-09-24. (1) a top contact is `hanging` only when the parent reaches more than `HANGS_BELOW_M` 0.05 m below the contact height (median of the contact points, parent frame); else `rests-on`, an abut, never a vote; a ref held by nothing else is free/water, counted `restsOnOnly` (`mine_mounts.py:184`, `:1023`–`:1029`, `:1166`–`:1173`). (2) a thin water vote whose every voting ref is no-contact water-column (source `column`) takes its policy row, `evidence: policy`, `votedClass` (`:1466`–`:1475`). (3) sample seed per asset, `Random(f"{seed}:{asset}")` (`:1374`); the new test fails on the old shared stream. (4) bamboohut01/02-with-door take the base plugin sink (−0.09/−0.11 m) from M14's `--complete-only`: for the 16i owner walk. Tests: plank on a top, seed independence, thin no-contact water. `rtree` was missing from the user site (declared in requirements-test.txt): installed | **Golden run 11: 25/25**; batch 5 re-scored 22/25; one combined run 243 s, peak 5.64 GiB cgroup. Ruling 1 result: `commoncounter01` wall 6 / deck 1 (hanging gone; 8 rests-on contacts); `kioskbarrierei01` still hanging 39 / deck 1: its foot is set INTO the plank (child z −0.154, plank −0.114..0.000, contact on the plank's top face, plank 0.113–0.128 m below it). Counter floor: `bauernhaus03innen`'s floor IS in the dumped mesh, 0.01 m from the counter bottom; the interior floor finder reads a static's bounds top (5.88 m for the one-mesh room) and the counter's bottom gives 2 side-classed samples. Ruling 2 over all 12 thin-water rows: `windchimehavok` and `dockstrent04` water → ground (policy); the other 10 unchanged. **Batch 6** (seed 20260933, expectations `/tmp/m15/batch6_expected.json`, written first): **21/25, FAIL**, no never-placed miss: `wrfence4way01` deck 2 / ground 1 (expectation wrong); `stockadescaffoldtop0sided01` ground 19 / deck 18 (under: kit `base0sided01` 5, non-kit `base0sided02` 3); `impwooddoorsinglesmallload01` free 6 of 9 (no candidate within 0.5 m, no frame); `mwimparchtowerbgtop01` free 1 (its tower is a candidate, not within 0.03 m). Protocol stop: full mounts run, sink run and manifest refresh NOT run. Tests: 61 passed / 4 red, the M14 four (manifest vs record 2, repository coverage, shipped-kit contract on the old record's `windchimehavok`) |
| M16 miner | planner rulings 2026-09-24 (support from below wins, 0085). (1a) a foot SET INTO a piece (child lowest <= piece lowest + `SET_INTO_M` 0.05 m, child top above piece top, >= `SET_INTO_TOP_SHARE` 25 % of the contact points nearest an up-facing piece triangle) is support, kind `set-into` (`mine_mounts.py:1049`, `:1113`, `SUPPORT_KINDS` `:1240`); (1b) the interior floor is a ray cast down (child −z) from up to 32 footprint vertices through each candidate's real mesh, up-facing hits only, 0.3 m reach (`floor_gap` `:1018`, `resolve_floors` `:1168`); the bounds-top finder is gone; (1c) hanging only with no side contact (`:1314`); (2) a door with no frame contact takes `CATEGORY_REF_CLASS` door → hanging, `hangingFrom: category` (`:207`, `:1265`); (5) sconce: the M15 golden n 1 is the 40-ref sample (seed per asset) of 298 refs; every ref gives band n 14 as in M14, so rests-on stole nothing; (6) `worldgen/check_requirements.py` (every requirements-test.txt module imports) as preflight gate `python-deps` and a CI step after both pip installs, test imports `rtree`. `worldgen/run_mount_batches.py` (the batch runner, kept). `mwimparchwall01destroyed02` `pad` row (yard K9 uses it; coverage red). Ruling 4: `route-spans-v1` should carry `stockadescaffoldbase0sided02` (yard lane: it now does, sink row mesh-sill 0.00). Tests: set into a plank, one-mesh room floor, side before top (fails on the round-15 rule), door with no frame, golden replay on the record (`test_the_record_holds_the_golden_set`; the fixture's claim that a test replayed it was false) | **Golden run 12: 25/25**; batch 5 **23/25**, batch 6 **23/25**, one run 217 s, peak 5.42 GiB; every miss discounted with its evidence in the fixture (kiosk ground: its plank `shipkatariahwoodfloor01` is non-kit; masts, `wrfence4way01`: M14/M15 expectation errors; `mwimparchtowerbgtop01`: ruling 3). `impwooddoorsinglesmallload01` hanging 7/9, `commoncounter01` ground 6/7. **Batch 7** (seed 20260935, expectations written first): **24/25**, 85 s; `dockstrent02` water discounted (mesh −6.23..+0.92 m, 3 refs 1.59 m above the water, 4.64 m of mesh under it). Hanging → other before the full run (sample, `/tmp/m16/diag_hanging.json`): first 10 checked by contact geometry: kiosk (set into the non-kit plank, right), `passend01` deck (set into the kit access, right), `shipbreticcarrack_halfwreck01` and `wrstairsplatform01` n 1 ground (set into rock/platform, right), `houseglandacceslatd01` and `histfruit` ground on a free/hanging tie (sample, not round 16), **`lantern02_blue` ground: set into `maginvlightspellart`, a light-effect mesh**, **`mudhut01intnew` deck n 1: a hut shell set into hay**, **`hayscatter04` deck 160 / ground 116: hay set into hay**, **`cedartree3`**: side before top turns 207 crown refs to wall. **Full mounts run**: 806 s, peak 7.22 GiB, 83,907 refs, 850 no-LAND dropped, 6,562 buried ground; ground 1,220 / deck 56 / water 50 / hanging 26 / fx 21 / wall 16; 173 pairs (band 37, points 136; the 21:14 record had 569); 86 assets change class vs 21:14, **hanging → other 22**. Sconce → `impfreewall01` band n 14, offset (0.083, 0.155, 2.719) m, yaw 90.2; `signwrdrunkenhuntsman01` → `signwrpost01` points n 2 (one ref, two posts). Sink `--complete-only`: p50 changed on 3 (mud hut-with-entrance −3.02 → −3.92, marsh-house-03 −3.65 → −4.31, both raw mesh tells from the other lane's rebuilds; `stockadescaffoldbase0sided02` new, 0.00). Refresh: 46 manifests rewritten, 44 changed. Tests: `test_mine_mounts` + `test_mine_designed_sink` + `test_check_requirements` 53 passed / 1 red (golden replay: `cedartree3` wall 231/367, share 0.63, over the ambiguity bar, so its water-zero row no longer applies); both manifest-vs-record tests green; `test_placement_metadata` 17 passed / 2 red: coverage (`expanded` empty, needs compiled output, M14) and the shipped-kit contract (25 findings, 13 water-class assets with no mined waterline, e.g. `dockcolstr02`, `stilthouseext`, the ferries, `stockadescaffoldbase0sided02` water 99/175) |
| M17 miner | planner rulings 2026-09-24. (1) non-kit statics under `effects/` and `magic/` are never candidates, any contact kind (`NON_SUPPORT_STATIC_DIRS`, `mine_mounts.py` scan); kit clutter never supports a shell, under, set-into or ray-cast floor (`supports`, `resolve_floors`); (2) policy rows decide: an asset row naming a class decides whatever the share, a kit row only an ambiguous vote of an asset with no asset row (`policy_rows`, `policy_override`); (3) a water class with no sink waterline carries `waterline` {p50, n, evidence}: cell water minus pivot over the defining file's column refs, n >= 3 (`column`), else the policy row's `fallbackSinkM` (`policy`; `water_column_waterline`); `apply_placement_metadata` reads it after the sink's; (4) `meshesMissing` summed over contact workers (`_measure_task`), ids in `meshesMissingIds`; (5) `placement_metadata.EVIDENCE_VOCABULARY` (term, fields, meaning) with the derived sets; both miners import it and validate against it; (6) `allowlist-determinism.json` rows for `run_mount_batches.py:57,62` (standard 6 green). Tests: clutter under a shell (the round-13 `_shell_on(False)` deck is now wall), effect/magic statics, policy rows, column/policy waterline, worker mesh count, writer reads the mounts waterline, vocabulary covers both records | **Sink full run**: 869 s, peak 7.97 GiB anon (cgroup, start 2.0); refs joined 59,261 → 60,030 (King of the Murkmire.esp joined via the yard lane's `asset_registry` pool change), static-supported 4,981 → 11,078 (set-into; expected +100..1,500, `/tmp/wf/round5/m17/sink_expected.txt`), plugin-unsupported 3,872 → 3,715; p50 changed on 130 (median abs 0.034 m): 104 non-KotM (median 0.030, max 2.03 `stockadescaffoldbase0sided02` mesh-sill 0 → plugin 2.03), 26 mwkeep (5 KotM rows with p50 5.7–9.3 m: `mwimparchdock02` 9.29, `ledgestairs01` 6.65, `foundation01` 6.63, `ledgecorout01` 5.98, `ledge02` 5.69); `pitchfork01` lost its p50; 8 waterlines changed (`dockcolstr02` −1.59, `stockadescaffoldbase0sided02` +1.70 new). **Golden run 13: 25/25**; batch 5 23/25, 6 24/25, 7 24/25 (same misses as M16, discounted); **batch 8** (seed 20260937, expectations written first): **21/25, FAIL**, 154 s, peak 4.73 GiB, meshesMissing 27 (19 `argonia/`, 3 `_resourcepack/` NIFs). Discounted: `scottish-pine22` water (sink waterline first), `rockl03wet` water 18/31 (bed under water), `commoncounterhanger01` ground (4 of 5 supported). **Real: `wickerchair01`** free 9 / ground 6: 8 interior refs find the `bamboohut01_int` floor by the ray cast (top 0.037 m) but the chair's lowest point is 0.122 m, a 0.085 m gap over CONTACT_M 0.03, so free not deck. **Protocol stop: the full mounts run was NOT made**; the record stays M16 (`cedartree3` wall, golden replay red). Batch-run rows for the 13 water assets: 11 carry a mounts waterline (column 0, policy 11), `dockcolstr02` and `base0sided02` take the new sink waterline; in-memory apply over the shipped manifests: 21 → 0 contract findings (nothing written). Ruling 1 result on sample: `lantern02_blue` ground 3 / hanging 3 / wall 3 / free 1 (share 0.4); `mudhut01intnew` stays deck (kit category `misc`, not a shell); `hayscatter04` deck 19 / ground 15 / hanging 6 (hay into hay untouched by the ruling). Tests: `test_mine_mounts` + `test_mine_designed_sink` + `test_check_requirements` 57 passed / 2 red (golden replay on the M16 record; manifest-vs-sink, 186 rows, until the refresh); `test_placement_metadata` 19 passed / 2 red (coverage, M14; shipped-kit contract, until the mounts run and refresh). Report `/tmp/wf/round5/miner.md` |
| M18 miner | planner rulings 2026-09-24. (1) an interior ray-cast floor within `FLOOR_SUPPORT_M` 0.10 m under the footprint supports (`support_from_below`; CONTACT_M 0.03 stays for side/top); (2) terrain touch wins for the designed sink: a reference whose lowest point (vertices within `FOOT_BAND_M` of it) is at most `TERRAIN_TOUCH_M` 0.05 m above the LAND, or below it, stays in the terrain sample whatever it overlaps or is set into (`touches_terrain`, `static_supported_refs`); a first cut on any vertex was re-run on the lowest point as written; (3) `fallbackWaterlineM` in `placement-policies.json` (water-zero 0.0, stilt 0.12, pad 0.18, dug-in 0.35; validated in `validate_policy_inventory`); `water_column_waterline` reads it, never `fallbackSinkM`, and a row without it gives no waterline; (4) `plugin_archives` + `nif_sources`: a non-kit NIF is looked up in its pool, then the BSA beside the pool's plugins, then vanilla. King of the Murkmire.bsa extracted from the vault 7z (837 MB, sha256 `fa64a221…36d6`, statistics only as K8), `lz4` installed for SSE BSAs (not in requirements-test.txt). Tests: chair 0.085 / 0.15 m over a ray-cast floor, lowest point vs higher vertex on a slope, set-into-and-on-the-LAND keeps its plugin sink, policy waterline (all four fail on the M17 rule), policy row validation | **Meshes**: 20 dumped from the KotM BSA (all 19 `argonia/` + `tamira/tropicalshrub05`); **genuinely absent from the vault** (every BSA and loose NIF searched): `_resourcepack/…/treebaldcypress02`, `treebaldcypressknee02`, `treewhiteelephantpalm02` (SSE `_ResourcePack.bsa`), `dlc02/…/dlc2telvannirootbridge01` (Dragonborn.bsa), `architecture/cyrodil/cyrlctavern`, `farmhouse/farmwall01`, `farmwallentrance02` (not in the vault's `Skyrim - Meshes.bsa`): sourcing note, the vault's vanilla archives are the LE set without the DLC/resource-pack BSAs. **Golden run 14: 25/25**; batch 5 23/25, 6 24/25, 7 24/25 (misses as M16/M17, discounted); batch 8 21/25: **`wickerchair01` still REAL**: its 8 interior refs now stand on the `bamboohut01_int` floor (0.085 m, free 9 → 1) but its kit category is `architecture` (htbm-hut-int config), so the shell rule makes it ground/interior-zero, not deck; one run 177 s, peak 4.77 GiB. **Batch 9** (seed 20260939, expectations written first): **22/25, FAIL**, 116 s, peak 4.23 GiB: `starfish` (8 interior refs on the non-kit `nobletablelong02`) and `mwimparchdockplatcorout01` (harbour edge, mesh −9.8..+2.5 m, 7 dry-LAND / 6 column / 6 set-into floor chunk, share 0.37) discounted; **`mudhut01intnew` REAL** (as M17: the hut shell set into kit hay, category `misc`). Protocol stop: the full mounts run was NOT made; the mounts record stays M16 (no `meshesMissingIds`, `cedartree3` wall). **Sink full run** (ruling 2, lowest-point form): 60,030 refs, static-supported 11,078 → **3,226** (M16 4,981; expected 5,000–6,500, `/tmp/wf/round6/m18/sink_expected.txt`), plugin-unsupported 3,715 → 3,674; p50 changed on 159 vs M17 (median abs 0.038 m), 6 by more than 1 m: `housetronc001` mesh-sill −23.87 → plugin +15.64 (n 4 of 20 refs, a 55 m trunk on cliff slopes, per-ref sink −220..+26 m), `nortmpextplattower02` −21.85 → −8.81, stockade scaffold bases 0 → +0.97/+1.96; stone walls and `rockl01` back on plugin rows (`stonewallendl01` 0.67 → 0.77, `rockl01` 1.60 → 1.70). Diff `/tmp/wf/round6/m18/sink_diff2.txt`; ~16 min, peak 8.6 GiB cgroup. **KotM rows** (`/tmp/wf/round6/m18/kotm_trace.json`): pivot at the mesh bottom on every ref; `mwimparchdock02` (mesh 11.2 m) pivot 8.4–9.4 m under the LAND, top 1.8–2.8 m above it; `ledge02`/`ledgecorout01`/`ledgestairs01` (8.0 m) 5.1–7.4 m under, tops 1.7–3.1 m above; `foundation01` (7.3 m) 6.1–7.1 m under, tops 3.6 m above: designed buried foundations, p50 unchanged (foundation 6.63 → 6.65, n 14 → 18). The yard's walls are not among them (the `mwimparchstonewall*` rows are mesh-sill). Tests: `test_mine_mounts` + `test_mine_designed_sink` + `test_check_requirements` 60 passed / 2 red (golden replay on the M16 record; manifest-vs-sink, 238 rows, until the refresh); `test_placement_metadata` 20 passed / 2 red (coverage, M14; shipped-kit contract, 22 findings, until the mounts run and refresh). Report `/tmp/wf/round6/miner.md` |
| M19 miner | planner rulings 2026-09-24. (2) terrain wins for the vote: a reference whose lowest point touches the LAND (`touches_terrain`, 0.05 m) votes `ground` (`water` on submerged terrain, round 11) even when set into or on a neighbour, source `terrain`, row field `terrainOverNeighbour` (`mine_mounts.classify_reference`, `classify_anchor`); test `test_a_foot_set_into_a_plank_and_touching_the_land_votes_ground` (fails on the M18 rule: deck both heights). (3) plugin sink rows with IQR > 1.0 m → mesh tell `plugin-spread`: **NOT coded**, it hits 90 rows (67 with a tell), rocks and plants included (`rockcliff02` 8.58 → −0.39 m; `/tmp/wf/round7/spread.txt`), against 0075's rock seating: brief § Open a2. (4) `lz4>=4,<5` in requirements-test.txt; `check_requirements` 11 of 11 import; kit-mining skill seeds and waterline evidence updated | **Golden run 15: 24/25, FAIL** (134 s, peak 3.3 GiB): `wrfencestr01` wall → ground 17 / wall 15 / deck 6 (M18 wall 15 / deck 13 / ground 10); the 7 `terrain` refs sit 0.009–0.727 m under the LAND set into a fence base. **Batch 10** (seed 20260941, expectations first): **24/25, pass** (1 discounted: `orcawninghalf01`, one ref, lowest point 1.25 m under the LAND, no contact: ground). Held batches on the same run (150 s, peak 4.4 GiB): 5 23/25, 6 23/25 (**`stockadescaffoldtop0sided01` REAL**: deck → ground 20 / deck 17, 8 refs 0.20–2.24 m under the LAND), 7 24/25, 8 22/25 (`wickerchair01` now furniture: passes), 9 23/25 (`mudhut01intnew` now architecture: passes). Protocol stop: **no full mounts run**; the mounts record stays M16. Sink `--complete-only`: 0 rows changed. Refresh not run (after the full run). Tests: `test_mine_mounts` 55/1 red (golden replay, `cedartree3` pair), `test_mine_designed_sink` 4, `test_check_requirements` 3, `test_kit_colliders` 4, `test_kit_compress` 8, `test_asset_taxonomy` 10 passed; `test_placement_metadata` 20/2 red (coverage `dungeon-root-v1`; shipped contract 21 findings). Fixture: golden run 15, held runs, batch 10. Per-ref traces `/tmp/wf/round7/m19/trace.json`; report `/tmp/wf/round7/lane.md` |

**BM&V terrain (resolved in round 9).** No terrain file is missing: Black Marsh.esm, Black Marsh North.esp and Valenwood.esp carry their LAND, and the reader reads it correctly. The swamp misreads came from three miner defects: bases defined in a master were never mined, a later plugin's LAND override and its references never met the master's cell, and water plants were tested against the LAND under them. Round 9 resolves forms through the masters, merges each cell over the load order, and classes a piece with nothing under it in a water column (cell water above the floor at its pivot) as water.

### Yard round K6 (2026-09-23 night; two deliver sessions, the first cut off at ~60 min)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| K6 found on disk | first session, 20:25–20:46, no ledger rows: (D) `collision` `mesh` on `ferryraft01`, `rowboat_ferry_swamp_01`, `canoe1` (mud, stilt) and `convex` on `signwrpost01` (imperial), the three kits rebuilt and compressed (20:32–20:36), gate `test_kit_colliders.py` (solid categories + hull/sign ids, `EXEMPT` rows with reasons, one synthetic kit that must fail 3 of 5); (E) `compile_settlement.anchor_quay_run` slides a `docks/quay-run-*` along its axis to the survey's wet edge; (B) `bind_doors_to_doorways` re-derives `thresholdUV` from the bound doorway, the door test reads the published bundle; (C) `dressing_host_at`: a ring prop over wet ground is dropped and counted (`dressingReport.droppedNoHost`); (F) skirt rewrite (`SettlementLayer.tsx` `treatmentMesh`: 0.9 m mitred offset, edges <= 1.83 m, per-vertex ground, alpha 1 → 0, polygon offset), rubble = flora kit `rockpiles01`–`04` under 2,000 triangles per building (`Groundcover.tsx` `foundationRubblePiles`, `foundationRubble.test.ts`), playbook line 32 and the treatments §2 state block; yard compiled 20:45 and exported 20:46 (55 place placements) | not done: (A) the assembly contact mine; the huntsman sign child |
| K6 yard | `test_proving_ground.py`: a published placement carries `kind` (`settlement`/`dressing`/`landmark`), never `objectKind`/`parcelId`, so the door test found no composite (red) and the over-water dressing test passed on an empty list; both now read `kind` and the parcel from the id, and the dressing test fails if dressing ids exist but none is read. Blueprint: `landmark.proving-ground.huntsman-sign` (`signwrdrunkenhuntsman01` at the sign post; prose not yet text-reviewed) | published bundle (20:46): door threshold to composite doorway farmhouse 0.00, mud hut 0.42, stilt house 0.00 m; quay landward tip 0.05 m from the dry/wet edge (shift 1.025 m), seaward tip 0.21 m from the raft's outline; ring dressing 41 placed, 17 dropped over water (hull berth 12, landing stage 5); proving ground + collider gate 16 passed, 2 failed (sign not published; three `barrel01` float 0.47 m, the plugin-sink defect); vitest `foundationRubble` 2/2, game-core `src/settlement` 35/36 (collider budget 1601 published vs 194 = round(worst × 1.55) from the rebuilt sidecars: stale until the export); typecheck 9/9 |
| K6 blocked | (A) `mwkeep` has no plugin (`asset_registry` pool `plugins=[]`; the vault folder holds `meshes/`, `textures/` and the .rar only); all 88 `mwkeep:` assets are `unplaced`, n 0, in `kit-mounts-mined.json`, so no plugin places the Imperial wall set and no abuts pair can be mined for it. (B) the mud hut's door stands outside its wall in the kit, not in the binding: BM&V places `hutexterior` at scale 1.30 (11 refs) and its `doorframe01` at 1.0 (44 refs); `mine_assemblies.local_offset` does not divide by the anchor's scale, so t0089/t0037/t0090 (radius 6.34/6.80/7.22 m, one bearing) are one doorway at three hut scales, and the composite puts the t0037 offset on a unit hut: frame 5.25–6.55 m and leaf 5.43–6.02 m out along +z against walls at 4.80 m (raw GLB). (C) the chairs, tables, totems and barrels are 97 decision 4 ring dressing (`dressing_count` by parcel `use`, the kit's `dressing[]` vocabulary), not mined-template children; a host-only rule would remove ground dressing from every place. (G) the recompile at 21:55 is red: the other lane's `kit-mounts-mined.json` (21:14) has no `impwallsconcecandle01` → `impfreewall01` pair (the golden fixture expects it), so the sconce has no parent and the export refuses the yard; the sign landmark adds 3 materials (98 > the yard's 96) | yard not re-exported or republished this session; `province:publish` not run |

### Yard round K7 (2026-09-23 night; deliver, ~110 min)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| K7 A scale | `mine_assemblies.unit_offset` (offset / anchor scale; `local_offset` unchanged, `mine_mounts` divides itself), `anchorScale` + `partScaleInAnchor` per template, schemaVersion 2, `assign_ids` keeps ids across re-mines, the `abuts` section survives a template re-mine. **K6 diagnosis corrected:** all 11 `hutexterior` refs are at 1.30 (none at other scales); t0089/t0037/t0090 are four `doorframe01` rings per hut at 6.34/6.67/6.92/7.22 m (a stepped porch), plus the leaf at 6.71 m. Unit frame: 4.88/5.13/5.32/5.55 m, leaf 5.17 m; they do not collapse to one offset (expectation error), they pair two by two at the 0.3 m tolerance (t0089 5.01, t0090 5.44). Full re-mine once: 6 min, peak 3.47 GiB cgroup | ids kept: vanilla 733/763, bmv-blackmarsh 351/358, bmv-valenwood 797/799, htbm 97/102; every id cited in kit configs, `compile_route_structures`, `test_render_assembly` survives except t0037 (merged into t0089; the mud config now cites t0089/t0090/t0091). Non-unit anchors: 39/111/44/66 templates. `bmv-blackmarsh:t0076` (`bridge01` chain) now 4.85 m, was 9.7 m (bridges at 2.0) |
| K7 A hut | `settlement-mud-v1.json` composite: hut + four frames + leaf at the unit offsets (scale 1.0); kit rebuilt, `measure_footprints`, `interiors_index`, `measure_connectors`, `kit_compress` (32.5 → 15.0 MB); yard footprint, door threshold and mud-hut path re-derived | door piece to hut surface (raw meshes): nearest frame 0.18 m (was 2.26), leaf pivot 0.38 m (was 2.16); entrance radius 5.01 m vs median wall 4.81 m; stilt leaf 0.031 m, farmhouse leaf 0.00 m; compiled door threshold to doorway: farmhouse 0.000, mud hut 0.001, stilt 0.000 m |
| K7 B abuts | `mine_abuts.py` (mine_mounts contact band, poses, patch classes; 2,000 samples; side patch only; child pivot outside the parent plan; plan offset + yaw clustered, rise a range); `compile_settlement.open_modular_ends` → `openModularEnds` (fixture: counted; province: WARN) | golden (vanilla wrfarmfence, enclosure-v1; Whiterun walls are in no kit; expectations `/tmp/k7_abuts_golden_expected.txt`): str↔str 3.64 n19, rail↔rail 3.64 n17, corner pairs n3; rail-on-footing rejected after the pivot rule; the t0460/t0461 end caps stand 1.37–1.41 m clear (co-placed, not touching). Fresh 1 (stonewall): split by slope rise → fixed → 5 pairs n15–32 (wall↔end caps ±3.56–3.63). Fresh 2 (newcastle, BM&V Valenwood): **FAIL**, 79 refs, 24 side contacts, 1 pair: scales 1.0/1.5/1.8/2.2 split wall↔wall (14.46 vs 13.59 m). Full run and `--write` NOT done. Yard: 30 open ends, all `no-abuts-evidence` (boardwalk 14, tower 4, stair 4, cave mouth 4, gate 2, wall 2) |
| K7 C/D/E | ring dressing off for fixtures (`bp_mod.is_fixture`); yard `maxUniqueMaterials` 98; tests: composite door leaf on its doorway (can fail on the pre-K7 offset), fixture carries no ring dressing; `compile_settlement --all` | yard 15 placements, 1 error (sconce: no mounts pair, miner lane), budget OK, dressing 0. pytest proving ground + mine_assemblies + compile_settlement: 66 passed, 4 failed (3 read the unexported bundle; `test_corrected_blueprint_compiles_clean` 6 errors, 1 from K7: its fixture's mud-hut door 1.36 m off the corrected doorway). Not exported or published. Table `/tmp/k7_yard_table.txt` |

### Yard round K8 (2026-09-23 night; deliver, Imperial wall evidence)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| K8 register | King of the Murkmire (SSE 190459, sha256 `7bf18067…a5ff2`, statistics only) found by the sourcing agent: `asset_registry` `mwkeep` pool `plugins` (was `[]`), `mine_assemblies.PLUGIN_POOLS` row (the abuts/assemblies join key), stale "no plugin" comment in `mine_designed_sink` fixed, sourcing-log row, README § Credits. Reimperialized 133861/134592/134616 in the vault, unregistered (re-pathed keys) | 133090's .rar holds no plugin (217 entries); KotM 318 exterior `mwimparch*` refs, 62 pieces, 14 curtain-wall refs, all in ArgoniaWorld. Tests: `test_asset_registry` + `test_mine_assemblies` + `test_mine_designed_sink` 17 passed |
| K8 abuts sample | `mine_abuts --set kotm --only …/keep/exterior/walls/` (expectations `/tmp/k8_abuts_imperial_expected.txt`, written first; output `/tmp/k8_abuts_walls.json`, `…_n1.json`); 6 s | 14 refs at two sites (scale 0.8 and 1.0), 13 candidates, 9 side / 4 under contacts, **0 pairs at min-count 3** (1 per key). Unit-frame joint consistent: every end-to-end contact at x ±7.25–7.28, y 0, yaw 0 (wall, destroyed walls, gate, tower alike): E1/E2 (tower inline)/E3 hold in form, fail on count. **Miner defect:** 4 of 9 end joints (wall→destroyed01 −7.27, gate→destroyed01 −7.25, tower→wall01 +7.28, destroyed02→destroyed01 −7.27) are patch class `under`, so dropped; the −x/+x asymmetry says the end face overlaps a lip. New form: back-to-back double wall (+y/+y, 2.81 m, yaw 180, 3 contacts, one key each). No end piece seen: both runs end on a destroyed wall. `--write` NOT run |
| K8 blocked | the ONE assembly parcel `parcel.proving-ground.imperial-wall-run`: a blueprint parcel carries one `assetRef` (`blueprint.py` 138–160; multi-piece only as kit composites, which `open_modular_ends` skips); no schema for a snapped multi-piece parcel exists, so its shape is a planner design call. Also no pair clears min-count 3 and no end piece is evidenced | yard not re-authored or recompiled; coordinate table unchanged |

### Yard round K9 (2026-09-24; deliver, abuts rulings A–E)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| K9 A miner | `mine_abuts.py` schemaVersion 2: relative scale in the pair key (`REL_SCALE_STEP` 0.1); end joint = pivot outside the parent plan AND (side patch OR every contact point within `FACE_BAND_M` 0.15 m of one side face); `family_of` (directory + name less trailing digits and `VARIANT_SUFFIXES` destroyed/damaged/broken/ruined/gate/tower) with `familyPairs`, `families`, family evidence in `endFaces`; `terminates`/`familyTerminates` (per ref, a bare face whose opposite abuts); `placedNoPairs`; natural pieces left out (`mine_assemblies` rule). Expectations `/tmp/k9_abuts_expected.txt` | sample (KotM curtain walls, 14 refs): 13 end joints (9 side + 4 face-band, was 9 + 4 dropped), family pair +x/−x 7.27 m yaw 0 **n 8** (expected 9: the ninth K8 joint is destroyed02→destroyed01 at 5.59 m, an authored overlap, n 1), back-to-back +y/+y 2.81 m yaw 180 n 4; piece pairs 0 at min 3. Fresh 1 newcastle: **pairs** (1024wall01 +x/−x 14.43 m n 3, family n 4; wall↔pillar relScale 1.2 n 3). Fresh 2 KotM stonewalls: 5 refs, 0 joints (too thin). Fresh 2b BM&V troncons: **fail on form**, 1 pair from 418 refs: 45° beams in square boxes chained by overlap (pivot inside the box, top patches). Fresh 2c vanilla stockade: pass (wallstraight +x/−x 5.81 m n 5, scaffold grid 3.6 m n 4–14). Full run `--write` once: 278 s, cgroup peak 5.34 GiB; 140 piece pairs, 136 family pairs, 52 families, 79 endFaces, 307 placed, 234 placed-no-pairs; `impfreewall01` not placed in any exterior (interior piece): no end pieces, stays lone |
| K9 B runs | `blueprint.py` parcel `pieces: [{asset, yaw?}]` instead of `assetRef` (schema text + validation); `blueprint_footprints.lay_pieces` (steps from piece + family pairs, relScale 1, either direction, forward cosine ≥ 0.7, most evidence wins; missing pair = error naming both), `laid_polygons_m`, `pieces_footprint` (union); `compile_settlement` lays a run (ids `….piece.N`, `run {index, length, pair}`, one pad, 97 B3 per piece outline), `open_modular_ends` reads family pairs + relScale, exempts run terminals with `terminates` evidence, reports `placed-no-pairs`. Yard: `parcel.proving-ground.imperial-wall-run` = destroyed01, gate, tower, destroyed02 (yaw 90, N–S on the west edge, gate arch facing east, the way re-routed from the gate) replacing gate/wall/tower; materials budget 98 → 106. Tests: family key, run stepping + missing-pair error | **4 pieces, not 7:** a 1 m scan of the whole yard finds no 7- or 5-piece run under the 2° 97 B3 limit clear of the other cells (best 5-piece 2.26°); 4 pieces 1.96°. Yard compile: 16 placements, 1 error (sconce, miner lane), budget OK, openModularEnds 14 (was 30): wall run 4 × `+y` (back-to-back face, not a run end), stilt stair 4 + cave mouth 4 `no-abuts-evidence`, boardwalk 2. Run ends met by terminates evidence; all ±x joints met by the compile's own `_offset_in` (frames agree). Table `/tmp/k9_yard_table.txt`. pytest proving ground + compile_settlement + blueprint + mine_assemblies: 144 passed, 4 failed (the same four as K7), then the 2 new K9 tests passed |
| K9 not done | C (composite audit against `anchorScale`/`partScaleInAnchor`, mud hut at 1.30, overhang05, wr-fence end caps, bridge01 t0076, kit rebuilds, `build_kit` post-steps) and D (mud hut footprint/threshold/path after C; `test_corrected_blueprint_compiles_clean` 6 errors) not started: budget spent on A/B/E | yard not exported or published; prose on the new parcel not text-reviewed |

### Yard round K10 (2026-09-24; deliver, planner rulings A–G)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| K10 A/B miner | `mine_abuts.py` schemaVersion 3: `joint_kind` (`run` = opposite faces with the plan offset along that axis, out of the parent's face; else `double`) on every row and pair; `endFaces` and `terminates` from run joints only, `doubleFaces` (information), `singleUse` (placed-no-pairs whose family has no run joint anywhere); docstring: overlap chains (BM&V troncons) are templates, not abuts. `compile_settlement.open_modular_ends` matches run pairs only and skips `singleUse`; `single_use_pieces` → `singleUsePieces` in the compile result. `test_mine_abuts.py` (7 tests, incl. the family `offsetSpreadM` > 0.3 m check that fails on purpose). Expectations `/tmp/k10/expected_sample.txt` | Sample (KotM walls): family +x/−x 7.27 m `run` n 8, +y/+y 2.81 m `double` n 4; wall ends ±x only. Fresh (vanilla wrfarmfence, min-count 1): `wrfencebaseend01` touches `wrfencebasestr01` once (−y/−x at 3.64 m, yaw 90), never at the t0460/t0461 yaw 0/180 offsets. Full run with `--write` twice (second after rounding the cluster centre before `joint_kind`: `kioskbarrierei01` 1.24/1.24 tie): 251 s, cgroup peak 4.50 GiB. Record: pairs 140 (run 59 / double 81), family pairs 136 (run 54 / double 82), `endFaces` 57, `doubleFaces` 49, placed 308, placed-no-pairs 257 = `singleUse` 257; max family spread 0.238 m. `stockadescaffoldbase0sided02` placed 33, in no pair. **Ruling B misses its target:** the yard's stilt stair (`passesc128h64d01`) and cave mouth (`doorcaveb`) are placed by no mined plugin (n 0), so they stay `no-abuts-evidence` (4 faces each) |
| K10 C | wall run stays 4 pieces (no 5- or 7-piece strip under 2° in the yard, K9 scan); for the owner walk | — |
| K10 D composites | audit of all 17 composites against the templates and abuts (`/tmp/k10/audit_before.txt`, `…_after.txt`). Re-authored: `settlement-mud-v1` hut part at 1.30, four frames + leaf at 1.0 at the placed offsets (unit × 1.30: frames 6.34/6.67/6.91/7.22 m, leaf 3.19, −5.92, −3.84 m); `hlaalu-domestic` marsh-house-03 `overhang05` 1.0 → 1.7 (t0011). Prose corrected: `route-spans-v1` marsh-timber (bridge01 IS a chain: t0076 anchorScale 2.0, 4.85 m unit, abuts run +y/−y n 13, 0.13 m overlap per unit length); `works-v1` 3-stage scaffold note claimed a deck top it does not carry. Agree, unchanged: quay-run-2/3, arblock masses, farmhouse01/02-with-door, stonewall-run-3/5, bamboohut01/02-with-door, scaffold 2/3-stage. `wr-fence-run-3` matches its templates (end caps at ±3.64 m, 1.37–1.41 m clear) and disagrees with abuts (caps in no pair): kept, planner call. No templates to audit: the two Anvil trees (hand-composed crowns), kiosk-with-access ×2, stilthouse-with-door (shared origin). `stockadescaffoldbase0sided02` added to `route-spans-v1`. `build_kit.measure_sidecars` runs `measure_footprints`, `interiors_index`, `measure_connectors` after every build, before compression | rebuilt `settlement-mud-v1` (45 assets, 32.5 → 15.0 MB), `hlaalu-domestic` (72, unpublished), `route-spans-v1` (23, 13.2 → 5.8 MB): 3 min, jobs 3, peak 4.81 GiB. Mud hut composite 15.11 × 15.11 × 9.45 m (hut alone 11.62 m). **Blocked:** `interiors_index.composite_doorways` copies the anchor's unit-frame doorway onto a composite whose anchor part is scaled, so the mud hut's entrance reads 5.01 m (median wall 6.26 m, frames 6.34–7.22 m): the file is outside this lane |
| K10 E | yard mud hut: footprint (`--apply`), district boundary (`--areas`), path (`street_router --apply`), `interiorClaim.sizeClass` medium → large (173 m²). `test_corrected_blueprint_compiles_clean` fixture (`testdata/place.fixture.mire-landing.json`), six errors: (1–3) 97 B3, hut-2 3.40° pad, quay-deck 3.78° stilt, warehouse 4.38° stilt, on the current ground: re-sited to the nearest ground clear of other parcels and ways (13 m / 16 m / 5 m; 1.27°/1.83°/2.63°), footprints, areas and thresholds re-derived (`--orient` turned hut-2 115° → 253.4° to face its path), path ends moved to the thresholds; (4) door 3 faced 255.7°, 10° off farmhouse02's only placed door (side 180° + yaw 85.7°): facing 265.7°, threshold re-derived; (5) door 4 1.36 m off the mud hut doorway: re-derived with `--orient` (sits on the mis-scaled entrance above); (6) budget 62 materials > 56 declared: the composites gained their door pieces (mud hut 20 materials): declared 64 | fixture compile: 0 errors; mud hut threshold still 5.0 m from the pivot in the yard (blocked above) |
| K10 F | `render_assembly.pose_hash` (sha256 of every piece's id, asset, parent, position, yaw, pitch, scale) in the stamp; test | `test_render_assembly` 47 passed / 1 red (`test_the_two_legibility_fixtures_were_rendered`: `/tmp/16h-lane-i` render fixtures absent, environment) |
| K10 G yard | `compile_settlement --all` | 16 placements, 1 error (sconce: no mounts pair, miner lane), budget OK; `openModularEnds` 8 (was 14): cave mouth 4 + stilt stair 4 `no-abuts-evidence`; wall run `+y` ×4 and boardwalk ×2 gone; `singleUsePieces`: boardwalk (`tamu_wooddock01`), sign post. Table `/tmp/k10_yard_table.txt`. `lint_prose --file` on the yard blueprint: 0 hits (no new prose written). pytest proving ground + compile_settlement + blueprint + mine_assemblies + mine_abuts: 153 passed, 4 failed (all read the unexported bundle: two door tests, huntsman sign, floats). Not exported or published |

### Yard round K11 (2026-09-24; deliver, planner rulings A–E)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| K11 A entrance scale | `interiors_index.composite_anchor_poses` (part 0 `scale`, `offsetM`, `yawDeg` from the kit config); `posed_mined_door`, `posed_link`, `posed_bounds_glb` carry the anchor's mined doorways, its plugin door link and its sibling door-piece boxes through that pose (`composite_doorways(parts, mined, pose)`, `classify_asset(anchor_pose=)`, `door_piece_doorways(pose=)`). Tests: pose maths (scale 1.3, yaw 90, lift 1 m), link + box, the mud hut on the real record (unposed reads < 6.3 m: the fail-on-purpose half). `settlement-mud-v1` re-indexed + sidecars republished (`kit_compress --sidecars-only`; GLB unchanged, kit-build table row 3). Yard: `--orient --apply --parcels` (yaw kept, 14.6° inside the 60°), via terminal moved to the new threshold, `street_router --apply`. Mire-landing door 4: threshold re-derived, routed polylines restored to K10's (the router ignores doors; its re-run made `hut-2-path` run THROUGH the hut), path end moved to the threshold (1.50 m). `test_the_door_leaf_check_fails_on_the_unscaled_offset` now fails on the unit-frame leaf (2.45, −4.55 m): K7's "bad" offset is today's right one | mud hut entrance 5.01 → **6.50 m** (inner frame pair; outer pair 7.06 m in provenance); yard door pivot-to-threshold 6.50 m, doorway gap 0.00 m; leaf-to-doorway 0.224 m (limit 0.5) |
| K11 B exporter evidence | `placement_metadata.py`: `PLACEMENT_EVIDENCE_FIELDS`, `GROUND_CONTACT_EVIDENCE`, `FIT_POLICY_PREFIX`, `fit_policy_evidence()` (the writer uses them; output byte-identical). `export_settlement_bundle._validated_asset_placement` imports them and `SINK_EVIDENCE_PREFIXES`; policy ids come from `placement-policies.json` (`known_policies`), not a copy; `POLICY_GROUND_FIT` gains `deck: direct` (host supplies the datum, as interior-zero). Handoff to the miner lane: `/tmp/wf/round5/handoff.md` | `tel_int_connector_01` validates as `deck` (test `test_every_placement_policy_the_writer_can_emit_exports`, which also fails if a new policy has no ground fit); 4 new fail-closed cases (sink term outside the vocabulary, unknown policy, fitPolicy naming another policy, ground-contact line) |
| K11 C single-use | `mine_abuts.derive_single_use`: structural kit pieces whose family has no run joint anywhere AND placed-no-pairs or unplaced; `placedNoPairs` stays raw; `--rederive` recomputes the record's derived set with no walk; `mine()` uses the same function | record `singleUse` 257 → 813 (556 unplaced structural pieces); yard `openModularEnds` 8 → **0**; `singleUsePieces` 5: boardwalk, cave mouth, sconce wall, sign post, stilt stair. Tests: 3 new (rule incl. non-structural excluded, record = derivation, a run piece with an unmet end stays flagged) |
| K11 D fence caps | `enclosure-v1` `wr-fence-run-3`: caps from the one mined straight-to-end joint (`wrfencebasestr01 −y` → `wrfencebaseend01 −x`, (0, −3.64) m, relative yaw 90 CW, K10 sample `s_fence.json`, n 1), far cap the same joint turned end for end. Measured on the meshes: Blender yaw 270 at −3.64 and 90 at 7.28 give 0.00 m gap, 40 contact points each; yaw 0/180 (templates t0460/t0461) 1.41 m gap; yaw 90 at −3.64 1.10 m. Note rewritten (length 12.5 m, was "7.3 m") | rebuilt + compressed: raw 31.6 → 13.9 MB published (was 34.6 MB uncompressed on disk), composite 1.04 × 12.50 × 2.00 m; build peak 8.0 GiB cgroup (other lanes live); no blueprint places the run. Sink `--complete-only` not run (miner lane's file) |
| K11 E yard compile | `compile_settlement --all` | 16 placements, **0 errors**, budget OK; table `/tmp/k11_yard_table.txt`. Tests: worldgen (proving ground, compile, blueprint, abuts, assemblies, export, colliders) 222 passed / 7 red, all reading the unexported bundle or the shipped register (door on the published doorway 0.711 m, huntsman sign, floats, farmhouse stilt vs plinth, known-red register, collider budget 1601 vs 143); `test_corrected_blueprint_compiles_clean` green. Pipeline 171 passed / 6 red: render fixtures (environment), placement coverage + shipped-kit contract (M16), and `test_kit_compress` ×3: 15 of the published kits are raw (not meshopt, bytes ≠ record: docks-v1 10.7 MB vs 3.67 MB recorded, bmv-treehouse-int, flora-province-v1 …), not written by this round; `enclosure-v1` and `settlement-mud-v1` pass `--check`. `lint_prose --file` on the enclosure config and the yard: 0 hits; `text-review` on the new note NOT run (no agent tool in this lane). Not exported or published |

### Yard round K12 (2026-09-24; deliver, three jobs)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| K12 A shell category | **not done, open (planner call):** `mudhut01intnew` sits in `mudmother-hut-int`, not `settlement-mud-v1`; a manifest's `category` comes only from the registry row (`build_kit.py:508`, `row.get("category", "misc")`), no kit config field overrides it, and `asset_taxonomy.classify` gives every `gv_meshes/argoniannest/` mesh `misc` at confidence 0.2 (`mudhut01` exterior shell too). Mechanism to choose: a per-entry `category` in the kit config read by `build_kit`, or a taxonomy rule plus a registry rebuild | no kit rebuilt |
| K12 B republish | `kit_compress --kit <k>` over the 15 kits that failed `--check` (of the published set, docs/FACTS.md), one at a time under memwatch, anon checked below 7 GiB before each (no waits needed) | raw → published MB: bmv-treehouse-int 7.8 → 3.3, docks-v1 10.7 → 3.7, dungeon-root-v1 41.6 → 18.9, flora-province-v1 80.5 → 26.9, htbm-hut-int 13.4 → 6.2, imperial-keep 20.6 → 7.3, mudmother-hut-int 23.7 → 11.6, route-structures-v1 18.9 → 9.2, ruin-monumental-v1 17.5 → 7.6, settlement-imperial-v1 23.0 → 10.3, settlement-root-v1 49.2 → 16.8, settlement-stilt-v1 37.8 → 15.4, underwater-v1 25.6 → 13.5, works-v1 37.2 → 17.2, xanmeer-interior-v1 19.8 → 7.7; every published size equals its compression record; published sidecars byte-equal the raw ones before the run (none changed). `public/kits` 487.0 → 235.5 MB; `site:compose` 405.6 MB after exclusions (warn 750, fail 900). Peak 8.67 GiB cgroup (flora-province-v1). `test_kit_compress` 8 passed; all 21 published kits pass `--check` |
| K12 C prose | `enclosure-v1` `wr-fence-run-3` note: "the one mined … joint" (`the-one-thing`), "are not used", "X, not Y" rewritten. Yard blueprint: `imperial-wall-run` whySpot ("the one 29 m strip … four pieces, not seven") rewritten; stale neighbour gaps re-measured on the blueprint footprints (edge gaps, 7,370 m per plan unit): stilt stair 8 m to the retired `imperial-gate` → 12 m to `imperial-wall-run`; wall run 13/25 → 12/23 m; imperial house ↔ mud hut 12 → 11 m (mud hut footprint re-derived in K10/K11) | `lint_prose --file` both files: 0 hits. `text-review` NOT run (no agent tool in this lane) |

### Yard round K13 (2026-09-24; deliver, rulings 1 and 5 with M19)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| K13 A categories | `asset_taxonomy._FOLDER_STEM_RULES` (confidence 0.7, tag `folder-stem-rule`): `gv_meshes/argoniannest/mudhut01*` architecture; its tables and shelves furniture, small props clutter; HTBM `villages/argonian/` wicker chair, sofa, table furniture, basket clutter, `chestwicker01` container. `asset_registry build`: 31 category changes, 26 from the rule (list `/tmp/wf/round7/registry_diff.txt`); the other 5 (`mwkeep` doors) and 156 `mwkeep` field rows are drift from the now-readable KotM plugin, not kept (brief § Open a6). Kits rebuilt + compressed (`--jobs 3`, 243 s, peak 4.3 GiB): mudmother-hut-int 22.4 → 11.6 MB, settlement-mud-v1 32.5 → 15.0, htbm-hut-int 13.2 → 6.2, settlement-stilt-v1 37.8 → 15.4, works-v1 35.1 → 17.2; sidecars re-measured on all five. `mudhut01` and `mudhut01intnew` ship `architecture`, collision `mesh`; test `test_the_mud_hut_shells_are_solid_and_collide` | `test_kit_colliders` 4 passed; `public/kits` 235.5 → 235.6 MB; `site:compose` 407.7 MB (warn 750, fail 900). Not re-run: settlement compile/export for the mud kits (`kit-build` §6) |
| K13 B raw-kit writer | the writer was `export_settlement_bundle --copy-assets` (`_stage_assets` copies `output/kits/<kit>.glb`, the raw build, over the published one, with the raw manifest whose compression record names the compressed size: K12 B's 15 kits). It now refuses any pair failing `kit_compress.glb_problems` (the `--check` rule minus sidecars, split out of `check`); test `test_copy_assets_refuses_a_raw_kit_build` (fails on the old copy) | every `output/kits` GLB is raw, so `--copy-assets` now refuses every kit: brief § Open c |
| K13 C record decides the fit | planner ruling 2026-09-24 (0085): a parcel `groundFit` is OPTIONAL and the kit record decides. `compile_settlement.POLICY_GROUND_FIT` (moved from the exporter, one map), `record_ground_fit`, `with_record_ground_fits` (a run takes its pieces' fit, which must agree; unresolvable = named error); `compile_blueprint` resolves once and hashes the authored blueprint; `compiled_blueprint_objects` fills an absent parcel fit from its placement (compile and export emit one record); `grade_settlement_pads.pad_specs` resolves absent fits (source hash stays the authored one); `blueprint.py` validates a fit only when authored. All 11 yard parcels repeated or contradicted their record: all 11 authored fits removed (the imperial house now `plinth`, y 24.38 m unchanged). `test_every_current_multi_fit_use_has_an_explicit_compatibility_rule` checks overrides only | `compile_settlement --all`: 16 placements, 0 errors. `export_settlement_bundle --copy-assets --fixtures-ok` REFUSED on the raw-kit copy (8 kits: docks-v1, dungeon-root-v1, imperial-keep, route-spans-v1, route-structures-v1, settlement-imperial-v1, settlement-mud-v1, settlement-stilt-v1; brief § Open c); nothing published, `province:publish` not run. pytest (proving_ground, kit_colliders, mine_abuts, compile_settlement, export_settlement_bundle, grade_settlement_pads) 140 passed / 6 red, all reading the stale published bundle or the abuts record: yard doors (mud hut 0.711 m), `signwrdrunkenhuntsman01` not published, 3 barrels float 0.47 m, register rows, collider budget 1601 vs 143; `test_the_record_single_use_is_the_derived_set` (K13 A category drift, `chestwicker01`). game-core 1277/1 red (collider budget 1601 vs 208, same stale bundle); typecheck green. Report `/tmp/wf/round7/publish.md` |

### Yard round K14 (2026-09-24; deliver, publish lane after the miner/yard decoupling)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| K14 A export copy | `export_settlement_bundle._stage_assets` (`:1269`) ships the published GLB and manifest from `public/kits` when they pass `kit_compress.glb_problems` and the published manifest equals the build manifest ("stale publish" refusal otherwise); a kit with no published GLB goes through `kit_compress.publish` first; a raw GLB in `public/kits` is still refused (M19 ruling 5). Test `test_export_settlement_bundle.py:587` | all 8 bundle kits pass `kit_compress --check` after the copy |
| K14 B scoped gates | the exporter validates only the (kit, asset) pairs the bundle places (`:335`, `:856`; `--all-kit-assets` at `:1366` keeps the catalogue form); `placement_metadata.bundle_used_assets` (`:466`), `shipped_contract_findings(roots, used)` (`:473`), CLI `--contract [--catalogue]` (`:647`); `test_placement_metadata.py:183` bundle-scoped, `PLACEMENT_CONTRACT_SCOPE=catalogue` runs the catalogue form | bundle 0 findings, catalogue 21 (miner lane's) |
| K14 C collider budget | `COLLIDER_PART_BUDGET` (`export_settlement_bundle.py:77`) 1601 → 155 by the 0052 rule on the fresh bundle; `settlementCollision.test.ts:129` counts collision kind `none` as 0 parts, as `SettlementLayer.tsx:287` builds no solid for it | yard worst 100 parts (TS counted 102 before) |
| K14 D truth table | `worldgen/fixtures/yard-truth.json` + `test_proving_ground.py:437-520`: 25 assets, 2 mount pairs, wall-run pitch 7.282 ± 0.15 m, expectations written from raw-GLB geometry before the records were read (`/tmp/k14_geom.json`); the test fails on a new mismatch and on a cleared one; can-fail proven | 6 mismatches in `knownMismatches` (brief § Part 1 state, Open yard 2); no policy rows added: the mechanism cannot change a mined class or sink. Sconce and huntsman pairs present; wall run published at 7.270 m pitch in order |
| K14 E abuts | `mine_abuts --rederive` | `singleUse` 813 → 814 (−`chestwicker01`, +`mudhut01`, +`mudhut01intnew`); nothing else changed |
| K14 runs | compile 16 placements, 0 errors, 0 warnings (peak 2.0 GiB); export exit 0, 2 pad grades pending; `province:publish` exit 0, 0.0 MB uploaded; yard table `/tmp/k14_yard_table.txt`; full report `/tmp/wf/publish/yard.md` | pytest (six files) 149 passed, 1 failed: `test_no_published_yard_piece_floats_or_misplaces_its_sill` (landing stage 0.47 m, stilt hut 0.17 m, limit 0.15 m). Pipeline 30/1 (coverage, miner lane). game-core 1286/1 (combat lane), `src/settlement` 36/36. text-review not run (no player-visible text) |
| K14 docs | check-in 2 yard packet written from the published bundle (brief § Owner check-ins); brief § Part 1 state "Open, in order" split into yard and miner lane; backlog row for the terrain height-blend shader | — |

### Check-in 2 fixes: yard (2026-09-24; deliver, rulings 5–12)

| Item | Delivered | Numbers / gaps |
|---|---|---|
| 5 stilt datum | `assetPlacement.deckClearanceM` (`placement_metadata.py` `deck_support_line`; support line = deck-top tell − clearance: sink for ground/deck, waterline for water); rows for `stilthouseext` + `composite:stilt/stilthouse-with-door` (ground, 3.462), `tamu_wooddock01` (1.14), `composite:docks/quay-run-2` (water, 0.672); the `stilthouseext` water-zero asset policy row removed | plugin median deck heights in asset metres (`/tmp/wf/checkin2fix/yard/deck_clearance.py`, `clearance_all.json`): stilthouseext 3.462 (n 4, BM&V), tamu_wooddock01 1.14 (n 35, HTBM, always scale 2.0), dockstrent01 0.547 (n 83), dockstrent02 0.711 (n 175), pooled docks 0.672. Yard stilt hut deck 3.46 m over mean ground (was 0.35), sink −4.733; render shows the stair foot at the ground line. Runtime wet case (support = water where it covers the legs) NOT in `anchoring.ts` (file outside the lane) |
| 6 composite visual step | `composite-author` §0 + step 14b (mandatory render of the BUILT composite, Sonnet list); stilt house leaf at offset (0, −3.065, −1.31), measured off the shell's doorway (jambs x ±0.8, sill −1.292); stilt-hut door record dropped, parcel `interior: none`; `blueprint.py` skips the needs-a-door check for `none` parcels | render `/tmp/wf/checkin2fix/yard/render/out` (before: leaf 1.29 m above deck, head in roof) and `out2` (after: leaf in doorway). The Sonnet reader did not run (the deliver agent has no agent tool); frames judged by the deliver agent. `render_assembly` has no cutaway view: queued |
| 7 hut | yard `mud-hut` parcel → `composite:stilt/bamboohut01-with-door` (HTBM, 17/17 load door at one offset), stilt district, door → `htbm-hut-int` medium, threshold on the leaf doorway (facing 148.0); empty `district.proving-ground.mud` removed | research recommendation 1 (`mudhut01` + `dlc2telmithryndoor01`) blocked: no Dragonborn BSA in the vault (`skyrim-source/Data` holds only the base BSAs); leaf gap 0.022 m |
| 8 boardwalk | cause: seated at the 0.35 m default deck clearance with 0.74 m of mesh above ground (not culled, published node present); now deck 1.14 m, top 1.50 m over the highest ground; `test_every_published_yard_piece_shows_above_the_ground_at_its_coordinates` (≥ 0.5 m) + its can-fail test | all 8 terrain-seated yard pieces pass; min top 1.50 m (boardwalk) |
| 9 cave mouth | `assetPlacement` `doorcaveb` sink −1.36 (floor band at the opening; mesh bottom −1.827); dug-in rule in `settlement-build` | sill 0.00 m above lowest ground (was 0.467 m). Flanks NOT embedded: 0 templates and 0 abuts pairs on doorcaveb in `kit-assemblies-mined.json`; terrain patch kinds (levee, bed-cut, terrain-request, poling-channel) live in `world/sources/terrain/`, outside this lane |
| 10 landing stage | `anchor_quay_run` slides the landward tip to where the deck plane (median water surface + `quay_deck_rise_m`) meets the ground; falls back to the wet-grid edge when no ground rises to the deck within 30 m | BLOCKED on the yard's ground: water level 17.93, deck 18.60, bank peak 17.71 within 30 m, ground at the tip 16.70: gap 1.90 m. Needs the docks kit's shore/steps piece (`dockstepsdownend01`, mined abut with `dockstrent01`, n 4) as a run end |
| 11 built ways | `modular-runs` §F; yard ramp `why.what` names it a labelled single piece | — |
| 12 notes | tower `mwimparchwalltower01` is the set's wall-height tower (taller: `mwimparchtowerbg01`); the sconce wall's open ends are expected (interior piece, no exterior evidence), kept as the mount exemplar | — |
| runs | stilt kit rebuilt (Blender + sidecars; memwatch killed the publish at a 9.29 GiB cgroup while the miner full run and a probe shared it), then `kit_compress` 37.8 → 15.4 MB (peak 2.2 GiB); `--refresh-built-manifests --kit docks-v1 --kit dungeon-root-v1 --kit settlement-stilt-v1`; yard compile 16 placements 0 errors, export exit 0, published | `test_proving_ground.py` 22 passed, 1 failed: leaf-in-doorway on the stilt house 6.256 m — the interiors index's open-front entrance sits on the +y side, the jamb-measured doorway on −y; `interiors_index` should take a composite's own leaf part as its doorway |

### Check-in 2 fixes: runtime (2026-09-24; deliver, rulings 1–4)

| Item | Delivered | Numbers / gaps |
|---|---|---|
| 1 cut | wall-foot skirt (`SettlementLayer.tsx` `treatmentMesh`) and rubble ring (`Groundcover.tsx` `foundationRubblePiles` and helpers, `foundationRubble.test.ts`) deleted; `GroundTreatment` narrowed to `id` + `footprintM` (the grass exclusion); treatments doc §2 and §3 items 19/21 marked cut | the exporter writes treatment rows as `id` + `footprintM` only since the publish round (below) |
| 2 flash | cause (research topic 2): the build effect's cleanup disposed the LIVE group and `revision` is a dependency, so every ≤ 40 m rebuild, every 2 s retry while terrain streamed and every kit arrival blanked the layer until the sliced rebuild swapped. Cleanup now only cancels; the group is disposed with the world (unmount, bundle, fatal); a retry that resolves exactly what is live is not swapped (`buildSignature`); one shadow twin per colour material, re-synced per build (three's shadow pass rewrites the twin's `side`), pruned at swap | per-frame `frames` evidence on the settlement proof (`blankFrames` must stay 0); `apps/world-studio/scripts/probe-settlement-flash.mjs` forces rebuilds through an injected `rebuildRef` |
| 3 camera | `FollowCamera.setObstruction((from,to,radius)=>distance|null)`: 0.3 m ball from the pivot, instant pull-in, 2.5 m/s return only on stick or player movement; `cameraCollision.ts` groups (settlement + terrain block, vegetation transparent); studio `cameraObstruction.ts` Rapier ball cast (sensors, dynamic, kinematic excluded); player fade 1.2 → 0.5 m (`playerFade.ts`, wrapper group); terrain clamp kept as fallback | interior camera is part 2 item 24 (research `docs/research/combat-and-systems/follow-camera-collision.md`); sandbox handoff `/tmp/lanes/combat/handoff-camera.md` |
| 4 windows | `blender/build_kit.py` reads the `Glow_Map_Texture` node (made only under SLSF2 Glow_Map) onto Emission; NiAlphaProperty test/blend → MASK at the NIF threshold for non-foliage (`alphaMaskMaterials`); `glowMaterials` in the manifest; runtime picks glow by `emissiveMap`, lights it in the emissive stage (mask × warm × night), night ramps on the sun's altitude (+2° → −6°) from `environment().epochMinutes` | `settlement-imperial-v1` rebuilt: 29 assets, 23.3 → 10.5 MB published (was 9.9), peak 4.73 GiB; 4 emissive materials (farmhouse01/02 and their composites), 28 masked (was 0); placement fields of all 29 assets unchanged against the pre-build manifest |

### Check-in 2 fixes: publish (2026-09-24; deliver)

| Item | Delivered | Numbers / gaps |
|---|---|---|
| publish | `yard-publish.sh` after both lanes: bundle byte-identical to the lanes' last publish (16 placements, 2 doors); `blueprints.json` re-exported (`test_committed_export_is_current`); check-in 3 packet (brief § Owner check-ins) matches the walk table row for row | table `/tmp/wf/checkin2fix/publish-walk-table.txt` |
| commits | runtime `5d854e98`; yard and kits `cbf94703` (the imperial and stilt kit configs also carry the building-breadth lane's added pieces, which the rebuilt manifests list); docs | review cc932960's four items fixed before the commits |
| preflight `--paths` | typecheck, npm-test, credits, water, rasters, python-deps pass | 5 placement reds + 1 pipeline red, none in these commits' reach: leaf-in-doorway (Open yard 0(a)); `test_mine_mounts` record-follows-rows (the yard's new `stilthouseext` rows wait on a miner run), golden set, `test_mine_abuts` single-use (miner lane); `test_interiors_index` KotM mud huts (KotM lane) |

### Check-in 3 fixes: kit and yard (2026-09-25; deliver, hand-off ruling 5)

| Lane | Delivered | Numbers / gaps |
|---|---|---|
| C3 decal | `blender/build_kit.py` `decalMaterials`; `set_alpha_modes` writes `decal: true` | rebuilt settlement-imperial-v1 (10 decal materials, 26.6 → 12.2 MB), settlement-stilt-v1 (38.0 → 15.6 MB), settlement-mud-v1 (36.2 → 16.9 MB), 0 decal each; peak 5264 MiB; the other kits with the flag (diagnosis count: vanilla-imperial-int 66, xanmeer-interior 47, wrecks 21, flora 15, enclosure 14, underwater 6, works 5) take it at their next rebuild |
| C3 yaw | one clockwise convention, converted once in `import_composite` and `pose_point_zup`; `template` named on 12 parts | built-geometry leaf gate: bamboohut01 5.8°, bamboohut02 5.6°, stilt house 0.2° off the doorway plane (bar 10°); the pre-fix turn measures 65.8° |
| C3 render check | turntable + plan of bamboohut01-, stilthouse-, farmhouse01-with-door, read by the lane agent (no separate Sonnet reader) | leaf in its doorway and in the frame's plane, no part through a roof or wall, leaf 2.0–2.5 m: yes for all three; farmhouse leaf foot sits ~1.5 m under the render's red ground line (the yard sill gate passes) |
| C3 stilt leaf | `composite_leaf_doorways`, rank `composite-leaf` | door-leaf gap 6.256 → within 0.5 m |
| C3 clearance | `vegetation_patches.settlement_clearance_patches`, 24 patches in `vegetation-patches.json` | yard A farmhouse apron: algrass03b at 0.52 m without them, none within 1.5 m with them (gate applies the published patches in memory); the bundles on disk stay the release (standard 6) and carry the patches from the next province publish |
| C3 gates | A/B gates in `test_proving_ground.py` and the patch staleness gate in `test_vegetation_patches.py` (brief § Part 1 state table) | `test_proving_ground.py` 38 tests, 37 pass; red: yard B wall run floats 0.35/0.32 m (16k backlog) |

### Skills (0086) (2026-09-24)

| Row | Delivered | Numbers / gaps |
|---|---|---|
| Skills (0086) | Four kit skills under `.claude/skills/`, each with a "Written against" header citing its ledger rows and records: `kit-build`, `kit-mining`, `composite-author`, `modular-runs`. `settlement-build` is the umbrella: a sub-skills routing table and step 8, the owner-walk packet; its "v1 pre-16h" banner replaced by what still waits for 16i. `routing-audit` step 5 checks every skill's citations (both modes). Citation headers added to `cost-review` (0079), `text-review` (0043, style guide, standard 12), `routing-audit` (0066, 0086 rule 4, standard 15) | `modular-runs` opens "Written … against", not "**Written against**", and cites K10 for the single-use set that K11 C re-derived |

### Gate fixes after the blueprint retirement (2026-09-24)

The authored waterway `waterway.hist-heartland.nine-trunks` (a canoe channel to the retired Nine Trunks dock) is retired from `world/sources/routes/authored-minor-waterways.json`; the row is in git at `df7798fe`. Tests that read the five retired blueprints now read the yard, a catalogue record or a blueprint written in the test; the list is in the commit message. Open: backlog row "`waterways-minor.json` is stale".

The authored waterway `waterway.hist-heartland.sap-tapping-licensed.landing` is retired the same way (planner ruling 2026-09-24); the row is in git at `f1e87b10`, and `authored-minor-waterways.json` now holds no rows. The two poling patches in `world/sources/terrain/terrain-patches.json` (`patch.poling.waterway.hist-heartland.nine-trunks`, `patch.poling.waterway.hist-heartland.sap-tapping-licensed.landing`) stay as frozen channel shapes: they are typed patches on the frozen ground (decision 0059) and survive the rows that authored them. The published `waterways-minor.json` was NOT refreshed: running `compile_minor_waterways` writes the water record, and no owner authorisation for that write was quoted (a dry run's diff is in the 2026-09-24 gate report). The mire-landing fixture's ways and the yard's three door paths were re-derived by `street_router` after two router fixes: a way ending at a doored parcel snaps to the door threshold and may enter the parcel only within `DOOR_CLEAR_M` of it, and every parcel a way may not enter is kept half the way's `widthM` off its centreline. The yard was republished with `yard-publish.sh`.

## 4. Sonnet sheet reports

**Pass 1 (2026-09-23, workflow `16h-sheet-pass-1`, Sonnet low, plan +
one elevation per item, checks: ground-line, float-or-bury, front-arrow,
doorway-marks, collider; mounts: hangs-from-right-part, no-intersection,
plausible-offset, orientation; assembly: abut, arch-open, tops-align,
yaw-consistent, ground-line).** Only the gate and three mount pairs were
read: the Haiku index step skipped the ten kit folders (re-run after the
re-render). Findings, all shared causes, none per piece:

- Lilmoth gate (flat): arch-open PASS; tops-align FAIL (wall crenellation
  below the tower parapet: judge again on a straight-on elevation);
  abut, yaw, ground-line UNSURE: the 30° perspective elevation has no
  legible metre marks and the red line is lost against dark stone.
- Mount pairs: all UNSURE, child indistinguishable from parent; and the
  three pairs read were a house interior on a house, a tree on a shrub, a
  hut door on a hut shell. Audit of the 35 pairs: all snap co-placements
  (scaffold tops, stair caps, doors in frames), because `mine_mounts.py`
  admitted every base object as a child; `mine_designed_sink.py` classed
  `wall` from geometry alone (bridges, magic stones, water lilies). Lane G
  re-derives both from placement evidence (rules in its brief).

**Pass 2 sample (2026-09-23, workflow `16h-sheet-pass-sample`, three
Sonnet readers, 14 sheets, 87 s).** The round-3 renderer reads: scale
bar, cut line and silhouettes legible on 13 of 14; the gate's ground
line, abut and float checks PASS on a straight-on elevation. Six shared
renderer defects found before any sweep (fixed in round 6, then the
sample re-read, then one sweep): the front marker is a dot with no
arrowhead; doorway dots blanket the whole canvas on some sheets (a job-
wide dot layer); a 30 m span frames to a few pixels (fixed extent, not
the piece's bbox); plan views carry no scale bar; composite sheets do
not say which piece is under test; the Lilmoth gate's collider view
covers the right tower and arch only. Also: a wall-hung piece alone
(the window pair) cannot be judged without its host, so templates ghost
their mined parent.

**Renderer rules produced (for the `kit-qa` skill, applied in step 3):**
elevations straight-on orthographic with a labelled metre scale; the
ground line a bright cut line on every elevation; mount children tinted,
parents faded; a Sonnet UNSURE on legibility means re-render, never judge.

**Sheets pass 2 (2026-09-24, workflow rounds 5, 6 and 7, render lane):
rendered 0 sheets, in no kit.** Sonnet verdicts
`{"right":0,"wrong":0,"unsure":0}`; 0 of the 173 pairs in
`kit-mounts-mined.json` have a sheet. Each round the render lane's
precondition failed because the publish lane stopped first
(`/tmp/wf/round7/render.md`: "Precondition failed … No render
attempted"). Round 6 stopped on the imperial house's authored fit
(`authored groundFit 'stilt' contradicts reviewed manifest policy
'plinth'`), fixed in K13 C. Round 7 stopped on the raw-kit copy (K13 B:
8 kits fail `kit_compress --check` in `output/kits`). The sweep waits on
brief § Part 1 state, Open b and c.

| Sheet | Reason class | Note |
|---|---|---|
| — | — | no wrong or unsure rows: no sheet was rendered |

## 5. Departures from the plan (pending)

## 6. Tooling speed lane (brief § Part 1 state)

**Step A, code read (2026-09-23, `research`, ran nothing).** The shared
cause: every process decodes the province survey from PNGs on its own
(~11 rasters at 1345², the 2017² water stack decoded TWICE (site_fields
:191 and compile_scatter :89), the 4033² vault height (62 MB), seven
distance transforms at start-up; ~0.7 GiB private per process, ~10 s+),
so compile, export, apply stages and pytest workers (capped at 2 by
`preflight.mjs:114`) cannot share memory and run serially. Ranked
changes (saving × confidence), the step B brief:

| # | Change | Expected |
|---|---|---|
| 1 | Survey cache: decoded arrays saved once as `.npy` keyed on `_survey_signature` (site_fields :949), opened with `np.load(mmap_mode="r")` by `ProvinceSurvey`, `ProvinceFields` and `ShippedWater`; read-only; rebuilt on any source mtime change (test guards staleness and byte equality with the PNG decode) | start < 1 s; 0.7 GiB private → shared page cache; compiles/exports side by side |
| 2 | Preflight: fold `placement` into wave 2, pytest workers to 4 (preflight.mjs :114-124) once #1 lands; re-measure peak | one wave fewer (~1 min of 2–3) |
| 3 | Sonnet render preset: `--res 256`, 3 views, fewer samples as a flag (render_assembly :692, :302; blender :151); owner sheets stay 512 | 26 min → ~6–9 min per full pass |
| 4 | Kit builds in parallel across kits (one Wine+Blender per kit, build_kit :936) after measuring one kit's peak; gltfpack stays `-tj 4` | rebuild of several kits ≈ max not sum |
| 5 | Render kit groups 2–3 sessions at once (render.py :631) | saves (groups−1) × 21 s |
| 6 | `mine_mounts` streams cells instead of `list()`-ing every ref (:380) | GiB-scale peak drop; output byte-identical |
| 7 | Heights-only land decode for the sink miner (esp_index :514-537 builds normals + texture layers nobody reads) | several × less per cell |
| 8 | Hoist record reads: export re-reads interiors/connectors/doorways per place (:998), compile reads doorways twice and rebuilds `KitShelf` per blueprint under `--all`; one `by_asset → kit` dict for the three small n² loops | seconds |

PARTIAL (needs one measurement each, taken inside step B since it runs
the tools anyway): the code accounts for ~1 GiB of the compile's 4 GiB
peak and nothing of the sink miner's 6 GiB (tracemalloc/memwatch on one
`--blueprint` and one sink run).

**Step B (2026-09-23, two `deliver` lanes, run between rounds 4 and 5
on the owner's instruction).** Peak = private anon of the process tree
(`/tmp/measure.py`), not the cgroup: the "4 GiB compile" and "6 GiB sink
miner" of rounds 1–4 were the WHOLE cgroup with other lanes' kit builds
inside it (a compile's own peak was 1.02 GiB, the sink miner's 0.26 GiB).
memwatch ceilings are cgroup-wide; size them to the tree, not the tool.

| tool | before wall / peak | after wall / peak |
|---|---|---|
| `ProvinceSurvey()` (survey cache: `.npy` under `output/survey-cache/province/<signature>/`, memmapped, atomic publish, signature over rasters + hydrology/routes/dressing records + the decoder modules; water stack decoded once; distance transforms cached lazily) | 10.2 s / 1.02 GiB | warm 1.3 s / 0.06 GiB (cold 12.8 s writes the 439 MB cache) |
| `compile_settlement --blueprint sap-tapping` | 14.8 s / 1.02 GiB | 6.8 s / 0.22 GiB |
| placement pytest wave (preflight: 4 workers at 1.5 GiB, placement folded into wave 2 → two waves) | 188.9 s / 3.07 GiB (2 workers) | 120.8 s / 3.76 GiB |
| `mine_designed_sink` (texture layers skipped; slope still needs normals so no heights-only path) | 33.1 s / 0.26 GiB | 32.2 s / 0.26 GiB (output byte-identical) |
| compile/export hoists (`KitShelf.kit_index()` once per compile, doorways read once, one shelf under `--all`, export records read once per run) | — | outputs identical; seconds |
| render one template / mount pair, owner preset → `--preset sonnet` (256 px, plan+front+right, 6 samples) | 16.8 s / 32.6 s | 13.3 s / 30.3 s (session start dominates) |
| `--all-templates --flat` 2 groups, `--jobs 1 → 2` (thread pool, one Blender per group; a session peaks 0.6–1.0 GiB, so up to ~6 fit in 7 GiB) | 36.9 s / 0.73 GiB | 22.7 s / 1.26 GiB |
| re-run with stamps current (`<safeName>.stamp.json`: kit GLB mtime+size + preset; a changed kit or preset re-renders) | frames-only check | 0.1 s, 0 sessions |
| `build_kit --kits mud,wrecks --jobs 1 → 2` (fork pool, default `--jobs 3`, per-kit work dirs, shared WINEPREFIX tolerates it; kit.json byte-identical) | 157.1 s / 1.23 GiB | 91.5 s / 1.99 GiB |

Known: Cycles output is not bit-identical run to run (≤ 12/255 on a few
pixels), so real renders compare by file names only; `survey.fields.water`
no longer carries the vault height (`refined`/`ground2` None; nothing in
worldgen read them); `mine_mounts` streaming is in round 5's miner lane;
`flora-province-v1` (77 MB GLB) peak unmeasured, build it alone.
Templates at 512 px, all 377, measured 28 min at jobs 1 in round 4; the
next full render reports the jobs-4 time here.

**Step C (targeted, when the compile is free; owner 2026-09-23):** the
Lilmoth compile (466 placements) took minutes in round 3 and the code
read does not explain it from the survey. Measure it on the cache; if it
is still over 30 s, `cProfile` it and fix the hot loop (suspects:
`validate_blueprint_full`'s 0.5 m fence sampling, blueprint.py:814,
867-960, and the shapely checks in `check_integration`). Every 16i/16j/15
place is compiled many times while its layout is iterated, so a city
compile is the daily bottleneck.

Step C result (2026-09-23): none of the four suspects; 29 of Lilmoth's
41 s were `validate_blueprint` re-routing every way through
`street_router.LocalField`, a pure-Python per-cell loop (17 fields,
2.8 M water samples, 490 k polyline distances), plus `astar`'s
`height_rc`. Vectorised with numpy, cell-for-cell equal to the old loop
by test on the stub survey and on every Lilmoth and sap-tapping way,
compile output byte-identical:

| blueprint | before wall / peak | after wall / peak |
|---|---|---|
| lilmoth (466 placements) | 40.8 s / 0.28 GiB | 4.8 s / 0.24 GiB |
| sap-tapping | 11.1 s / 0.22 GiB | 4.4–6.9 s / 0.22 GiB |

Left: the survey start-up is now the compile's biggest cost and the
cache sometimes rebuilds (11 s in one profiled run); step D logs hit/miss.
Three pre-existing reds (Mazzatun fence C10, 17 stale known-red rows,
committed export not current, collider budget 1600 vs 1973) are the
pre-preflight fix-up's, not this lane's.

**Step D (owner 2026-09-23: "how do we know what else needs optimising?"):**
a mechanism, not a list. `memwatch.sh` appends one line per run
(`tool, args, wall s, peak anon GiB, exit, date`) to
`tooling/repo-standards/output/tool-timings.jsonl` (gitignored), and
`python3 tooling/repo-standards/tool_timings.py` ranks tools by total
and by worst single run over the last N days. The tooling lane reads that
ranking; a tool that tops it twice gets a step-C-style profile. Runs with
`--jobs`/`--preset` record them so the effect of a default shows up.

Step E result (2026-09-23; the terrain patch stage and the raster
publish were NOT run: owner rule, nothing above 16h is re-run):

| stage | before wall / peak | after wall / peak |
|---|---|---|
| `apply_vegetation_patches`, shipped set (183 patches, 162 chunks, 519 pairs), 3 workers | killed at 15 min, ~50 min extrapolated | 61.8 s after batching, **11.9 s / 0.31 GiB** after the polygon bbox cull (output byte-identical) |
| one chunk (4_3, 10 patches, 2,584 plants) | 178.6 s | 3.6 s, then 0.40 s |
| studio `vite build` (CI only; preflight has no build gate) | 9.0 s, 5.0 s of it copying the 423 MB public/ tree | unchanged |

Hot spot: `_inside_and_distance` called 6,700 times per chunk (species ×
5 reach points × 2 lists × patches), each walking ~950 polygon edges;
now one batched `keep_field` call per patch per chunk. Left: ~60 s in
the same per-edge loop on ~13k points; polygon bbox culling beyond the
saturation distance is the next cut (same lane). Memory guards in
briefs must name anon + shmem (what memwatch reads), never
`memory.current` (file cache made it read 10.8 GiB on an idle box).

**Step E (as briefed; owner 2026-09-23 "what else is not sped up yet?"):** the stages part 1 never ran and part 2, 16i and
16j run for every place: the vegetation scatter behind
`apply_vegetation_patches` / `compile_scatter` (the longest job in 16f),
the terrain patch chain stage (`settlement-pad`) and the province raster
publish (`chain_stages run`, one survey per stage), and the Vite studio
build inside preflight (~2 min per commit). Measure each on the survey
cache, profile any over 60 s, fix the hot loop; table here.

## 7. Owner check-in 1 (2026-09-23)

The owner walked the yard on 2026-09-23. Bases were good. Stilt and quay
pieces stood a leg-length too high, the Imperial wall set stood as lone
hollow parcels, and the mud hut's door stood apart from the hut. Stray
chairs and tables stood with no host, and the ferry raft and sign post
had no collider. The findings with coordinates, the five causes and their
fix rules are in the brief § Part 1 state (16h brief).

**Ladder note.** `ladder.json` was written from the fresh stamps, through
16h. The chain's own ladder write could not run: `compile_minor_waterways`
tripped on a dock authored for the retired Nine Trunks blueprint. That row
was retired 2026-09-24 (§ Gate fixes); the compile runs again.
